# CLAUDE.md

سیستم مولتی‌ایجنت تولید محتوای **فومنت (Fuwment)** — Next.js 14 (App Router) + TypeScript.

پاسخ‌ها و کامنت‌های کد **فارسی** هستند. این قرارداد پروژه است، نه ترجیح لحظه‌ای.

---

## ⚠️ اول این را بخوان: کجا اجرا می‌شود

| کار | کجا |
|---|---|
| **اجرای پایپ‌لاین** | روی **لپ‌تاپ**، با `npm run dev` |
| **استودیو** (تماشا، تأیید، ارسال به وردپرس) | Vercel |
| **داده** | Supabase — هر دو طرف به همان دیتابیس |
| **بلاگ عمومی** | وردپرس روی `fuwment.com` |

یک اجرای کامل **بلاگ** ۱۷۵ تا ۲۹۳ ثانیه طول می‌کشد و سقف تابع در پلن **Hobby** ۳۰۰ ثانیه است، بدون امکان افزایش.

⚠️ این عدد **فقط مال بلاگ** است. پایپ‌لاین‌ها هزینه‌ی یکسانی ندارند و برون‌یابی از روی بلاگ، عددِ چند برابر غلط می‌دهد:

| پایپ‌لاین | فراخوانی مدل | زمان اندازه‌گیری‌شده |
|---|---|---|
| بلاگ | ۱۰ تا ۱۵ | ۱۷۵ تا ۲۹۳ ثانیه |
| **اینستاگرام** (فارسی، بدون بازنویسی، **با رندر و تصویر**) | **۵ متن + ۱ تصویر** | **۴۶ تا ۵۱ ثانیه** |
| اینستاگرام — عدد تاریخی، **قبل از** گام رندر | ۵ | ۳۴ ثانیه |
| بازآفرینی · لینکدین · ریلز · کمپین | — | هنوز اندازه‌گیری نشده |

تفکیک دو اجرای واقعی (۲ شهریور ۱۴۰۵)، بر حسب ثانیه:

| گام | اجرا ۱ | اجرا ۲ |
|---|---|---|
| ایده‌یاب اجتماعی | ۱۰٫۴ | ۱۸٫۵ |
| استراتژیست | ۵٫۸ | ۵٫۱ |
| کپی‌رایتر | ۵٫۸ | ۱۵٫۳ |
| ویراستار | ۲٫۷ | ۲٫۵ |
| ناشر | ۰٫۲ | ۰٫۲ |
| **رندر اسلایدها** (شامل ۳٫۸ / ۲٫۸ تولید تصویر) | **۷٫۵** | **۵٫۷** |
| منتقد | ۱۰٫۹ | ۲٫۲ |
| **کل** | **۴۶٫۵** | **۵۰٫۶** |

⚠️ **پراکندگی گام‌ها زیاد است** — کپی‌رایتر بین ۵٫۸ و ۱۵٫۳، منتقد بین ۲٫۲ و ۱۰٫۹. با دو نمونه نمی‌شود میانگین قابل اتکا داد؛ برای برنامه‌ریزی ظرفیت از عددِ **بالا** استفاده کن، نه میانگین.

هنوز هم بیشتر زمان اجرا انتظار برای مدل است، ولی دیگر «بیش از ۹۵٪» نیست: گام رندر ۵٫۷ تا ۷٫۵ ثانیه می‌گیرد که حدود ۳ ثانیه‌اش تولید تصویر (فراخوانی شبکه) و بقیه‌اش رندر واقعی روی CPU است. **رندر دوباره‌ی همان پست ارزان‌تر است** چون تصویر پس‌زمینه از Storage خوانده می‌شود و فراخوانی جدیدی نمی‌رود.

اینستاگرام پژوهشگر (کندترین گام بلاگ، ۳۶ ثانیه) را اصلاً ندارد و نویسنده‌اش هم `LONG_FORM_TIMEOUT_MS` نمی‌گیرد.

⚠️ **این عدد به مدل وابسته است.** با `google/gemini-3.7-flash` اندازه‌گیری شده (پایین‌تر را ببین). عوض‌کردن `PIPELINE_MODEL` یعنی این جدول باطل است.

**نتیجه‌ی طراحی: موازی راحت جا می‌شود، ترتیبی دیگر نه.**

این با نسخه‌ی قبلی این فایل فرق دارد و دلیلش گام رندر است. با ۳۴ ثانیه، ۷ × ۳۴ ≈ ۲۳۸ ثانیه زیر سقف بود. حالا **۷ × ۵۱ ≈ ۳۵۷ ثانیه، از سقف ۳۰۰ رد می‌شود** — حتی بدون هیچ بازنویسی. با یک دور بازنویسی بدتر هم می‌شود.

پس اگر روزی تولید دسته‌ای به Vercel منتقل شد:
- **موازی**: مسئله‌ای نیست، سقف روی طولانی‌ترین اجرا اعمال می‌شود نه مجموع.
- **ترتیبی**: سقفِ امن حدود **۵ اجرا** است، نه ۷. محافظ مهلت `run-steps.ts` اجرای ششم را تمیز متوقف می‌کند، نه با کشته‌شدن — ولی متوقف می‌کند.

و یادآوری: پایپ‌لاین معمولاً روی **لپ‌تاپ** اجرا می‌شود، جایی که اصلاً سقفی نیست. این عددها فقط وقتی مهم‌اند که کاری به Vercel منتقل شود.

برای بقیه‌ی پایپ‌لاین‌ها یک اجرا بگیر و لاگ `[timing]` را بخوان.

**`waitUntil` این را حل نمی‌کند.** مستندات Vercel: «promiseهای داده‌شده به `waitUntil` همان timeout تابع را دارند» و مهلت «شامل پردازش درخواست و کارهای ناهمگام `waitUntil`» است. زودبرگرداندن پاسخ، کار را از سقف معاف نمی‌کند. اگر دوباره به این فکر افتادی، همین‌جا متوقف شو.

اگر روزی پلن Pro شد: `maxDuration` تا ۸۰۰ بالا می‌رود و اجرا روی Vercel ممکن می‌شود.

**صفحه‌های `/blog` در این پروژه پیش‌نمایش داخلی‌اند، نه بلاگ عمومی.** noindex هستند و پشت رمز.

---

## دستورها

```bash
npm run dev          # اجرای محلی — پایپ‌لاین را از اینجا اجرا کن
npm run build        # بعد از هر تغییر. کاربر همیشه این را می‌خواهد
```

`npm run build` تنها دروازه‌ی کیفیت است (typecheck + lint). تست خودکار وجود ندارد.

**برای تست منطق خالص** بدون بالاآوردن سرور: فایل را با `tsc` جدا کامپایل کن و در Node اجرا کن. مسیرهای `@/` با `Module._resolveFilename` نگاشت می‌شوند. الگویی که در این پروژه جواب داده:

```bash
# $OUT = پوشه‌ی موقت. اگر نشست پوشه‌ی scratchpad اختصاصی دارد، از همان
# استفاده کن، نه /tmp — دستور محیط بر این فایل مقدم است.
OUT=/tmp/x
cat > tsconfig.tmp.json << EOF
{ "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": false, "module": "commonjs", "target": "es2020", "outDir": "$OUT" },
  "include": ["src/lib/agents/brand-checks.ts"] }
EOF
./node_modules/.bin/tsc -p tsconfig.tmp.json; rm -f tsconfig.tmp.json
```
خطای `TS5095` (bundler) را نادیده بگیر — خروجی تولید می‌شود.

⚠️ خروجی را داخل ریشه‌ی پروژه کپی کن (`.tmp-x/`)، وگرنه `node_modules` پیدا نمی‌شود. بعد پاکش کن.

---

## معماری

```
ایده‌یاب → استراتژیست → پژوهشگر → نویسنده ⇄ ویراستار → سئو → ناشر → وردپرس → منتقد
```

- **ارکستریتور کد است، نه LLM.** جریان کار، شرط‌ها و حلقه‌ها در `orchestrator.ts`. هرگز orchestration را به مدل نسپار.
- **هر گام بلافاصله در store آینه می‌شود** (`run-steps.ts`) — به همین دلیل استودیو می‌تواند اجرای محلی را زنده تماشا کند.
- `makeStepRunner` روی throw **کل اجرا را می‌کشد**. گامی که نباید اجرا را بشکند (منتقد، وردپرس) باید داخلاً try/catch کند و همیشه `{ output, summary }` برگرداند.
- چهار پایپ‌لاین اجتماعی از `social-loop.ts` مشترک استفاده می‌کنند.

### فایل‌های کلیدی
| فایل | نقش |
|---|---|
| `src/lib/company.ts` | **مالک کاربر.** هویت برند، `BRAND_CTAS`، `BLOCKED_SOURCE_DOMAINS`. با احتیاط ویرایش کن |
| `src/lib/agents/brand-checks.ts` | چک‌های قطعی برند، بدون LLM |
| `src/lib/agents/brand-cta.ts` | رندر CTA برای پرامپت + `ROUTE_BRIEFING` (متن راهنمای هر مسیر) و re-export `BrandRoute`. **تعریفِ اصلی مسیرها در `company.ts` است، ولی توضیحِ پرامپتی‌شان اینجاست** |
| `src/lib/ai.ts` | تنها نقطه‌ی تماس با مدل. سقف زمان و retry اینجاست |
| `src/lib/wordpress.ts` | انتقال به وردپرس |
| `src/lib/auth.ts` + `src/middleware.ts` | محافظ استودیو |

---

## قواعد سختی که با هزینه یاد گرفته شدند

هر کدام از یک شکست واقعی درآمده. برنگردان.

### ۱. کار قطعی را به مدل نسپار
شمردن کاراکتر، برش متن، یکتاسازی اسلاگ، چسباندن بخش‌ها، تاریخ، فهرست منابع — همه در کد. مدل فقط قضاوت می‌کند.
- `clampText` در `types.ts`، نه `.max()` در Zod
- تاریخ و منابع در `article-format.ts` **بعد از** چک‌های برند اضافه می‌شوند (وگرنه ارقام لاتین تاریخ میلادی، چکِ «ارقام فارسی» را می‌شکند)

### ۲. چکِ قطعیِ غلط بدتر از نداشتن چک است
بازنویسی بی‌دلیل راه می‌اندازد و به مدل می‌گوید چیزی را درست کند که خراب نیست. قبل از افزودن هر چک، **خطای کاذبش را تست کن**.

نمونه‌های واقعی که نزدیک بود بشکنند:
- «کار ما این است که این تفاوت را زود مشخص کنیم» — جمله‌ی **مجازِ** برندگاید. الگوی مقایسه با رقبا باید وجود «با … متفاوت» را الزام کند، وگرنه همین جمله رد می‌شد.
- «در بهترین شکل ممکن» — جایگزین **مجازِ** برند برای ادعای تضمین. `بهترین` استثنا دارد.
- `weforum.org` — تطبیق زیررشته‌ای «forum» منبع معتبر را می‌کشت. تشخیص انجمن با **برابری برچسب** است، نه زیررشته.
- «مشاور مهاجرتی ثبت‌شده» — خودِ برندگاید الزامش کرده؛ تطبیق سرراست «مشاور» ردش می‌کرد.

### ۳. مرز واژه در فارسی
`\b` در جاوااسکریپت روی فارسی کار نمی‌کند. مرز را دستی بساز:
```ts
const WORD_CONTINUATION = "[\\u0600-\\u06FF\\u200CA-Za-z]";
new RegExp(`^${term}(?!${WORD_CONTINUATION})`)
```
بدون این، «مالیات»، «مانند»، «ماه» با «ما» تطبیق می‌خورند. و پسوند فارسی می‌چسبد: «فوق‌العاده‌ای» با تطبیق کلمه‌کامل گرفته نمی‌شود — `wordWithSuffix` برای همین است.

### ۴. تفکیک شدت چک‌ها
`blocking` بازنویسی می‌سازد؛ `advisory` فقط گزارش می‌شود و جلوی انتشار خودکار را می‌گیرد.

در یک اجرای اندازه‌گیری‌شده، واژه‌ی «مشتریان» دو دور کامل (۶۶ ثانیه از ۲۰۱) هزینه ساخت در حالی که ویراستار از پاس اول تأیید کرده بود.

- blocking: ادعا، مقایسه با رقبا، برند در تیتر/فاعل، روایت دشمن، قاب‌بندی ترجمه، صفت مطلق
- advisory: واژگان برند، ارقام فارسی، گیومه، نیم‌فاصله

⚠️ این فهرست **فقط فارسی** است. متن انگلیسی فهرست جداگانه‌ای دارد — قاعده‌ی ۵.

### ۵. لایه‌ی چک، دوزبانه است — و هر سه تصمیمش با هزینه گرفته شد

`runBrandChecks({ text, language })` برای `"en"` به `runBrandChecksEn` شاخه می‌زند
(`brand-checks.ts`). دو فهرست جدا، نه یک فهرست ترجمه‌شده.

**فارسی ۱۱ چک · انگلیسی ۸ چک.** بیشترِ تفاوت عمدی است، یکی نه:

#### الف) سه چک نگارشی فارسی + اصلاح خودکار، برای انگلیسی خاموش‌اند

`ارقام فارسی` · `گیومه‌ی فارسی` · `نیم‌فاصله` — و جدول `applySafeBrandFixes`.

روشن‌کردنشان یعنی **هر عدد لاتین و هر `"` در یک متن انگلیسیِ کاملاً سالم رد
می‌شود**. نتیجه: هیچ پست انگلیسی هرگز چک‌لیست تمیز نمی‌گیرد، انتشار خودکار
برای همیشه قفل می‌ماند، و ایرادهای کاذب به پرامپت ویراستار تزریق می‌شوند.
این دقیقاً قاعده‌ی ۲ است، فقط از سمت زبان: چکی که روی متن سالم می‌افتد،
بدتر از نبودنش است.

#### ب) حفره‌ی شناخته‌شده: فقط `برند فاعل پاراگراف` گم است

از دو چکِ قاعده‌ی ۸، **`برند در تیتر بخش` در فهرست انگلیسی هست** — آخرین
عنصر آرایه‌ی برگشتیِ `runBrandChecksEn`. بدون تغییر بازاستفاده می‌شود چون
روی `BRAND_NAMES` کار می‌کند و `COMPANY_NAME_EN` («Fuwment») در همان فهرست
است.

گم‌شده فقط `برند فاعل پاراگراف` است، و **دلیلش تصمیم طراحی نیست**:

```ts
const BRAND_SUBJECT_STARTERS = ["ما", ...BRAND_NAMES];
```

افزودن `We` و `Our` یعنی ویرایش یک **ثابت مشترک با مسیر بلاگ**. قرارداد
ایمنی می‌گوید روی فایل‌های مشترک export جدید بله، ویرایش export موجود نه —
پس به تعویق افتاد، نه اینکه لازم نباشد.

تا آن موقع، «شروع پاراگراف با We/Our» در متن انگلیسی فقط به روبریک ویراستار
سپرده شده. اگر روزی حلش کردی، راه امن این است که یک ثابت جدا برای انگلیسی
بسازی، نه اینکه به این یکی دست بزنی.

#### ج) `checkLanguageMatch` وجود دارد چون یک بار واقعاً اتفاق افتاد

`social-checks.ts`. متن فارسی بود و بریف `"en"` می‌گفت، پس فهرست **انگلیسی**
روی متن **فارسی** اجرا شد. هیچ واژه‌ی ممنوعی پیدا نکرد — چون دنبال
`guarantee` و `solicitor` می‌گشت و متن فارسی بود.

**۱۶ از ۱۶ چک سبز شد، در حالی که هیچ گاردی وجود نداشت.**

شکست بی‌صدا با ظاهر موفقیت — بدترین حالت ممکن، چون هیچ‌کس نمی‌رود سراغ
چک‌لیستی که کامل پاس شده. برای همین این چک **اول از همه** در `allChecks`
اجرا می‌شود (`social-loop.ts`) و `blocking` است: اگر زبان نخواند، نتیجه‌ی
بقیه‌ی چک‌ها بی‌معنی است.

آستانه‌ها **نامتقارن‌اند و باید بمانند**: فارسی `ratio >= 0.5`، انگلیسی
`ratio <= 0.15`. متن فارسیِ سالم واژه‌ی لاتین دارد («Global Talent»،
«Innovator Founder»)، ولی متن انگلیسیِ سالم تقریباً هیچ حرف فارسی ندارد.
متقارن‌کردنشان یکی از دو طرف را می‌شکند.

#### د) ویراستار باید زبان‌آگاه بماند

`social-editor.ts` روی `brief.language === "en"` شاخه دارد. قبل از اصلاح،
ویراستار به کاروسل انگلیسی `persian: 0` می‌داد و در `issues` می‌نوشت
«به فارسی برگردان».

یعنی **ویراستار به کپی‌رایتر دستور می‌داد چیزی را خراب کند که چک قطعی
بعداً ردش می‌کرد** — و چون شکست چک به‌تنهایی بازنویسی اجباری می‌سازد
(قاعده‌ی ۴)، سیستم وارد یک دور بازنویسیِ کاملاً بی‌حاصل می‌شد.

اگر روزی زبان سومی اضافه شد، هر سه لایه باید هم‌زمان عوض شوند: فهرست چک،
`checkLanguageMatch`، و روبریک ویراستار. یکی را جا بگذاری، همان شکست
بی‌صدای بند (ج) برمی‌گردد.

### ۶. شکست باید صدا داشته باشد
`if (!res.ok) return []` بدون لاگ، یک روز وقت گرفت: کلید Tavily یک حرف اضافه داشت (`ttvly-`)، API با ۴۰۱ جواب می‌داد و پایپ‌لاین بی‌هیچ نشانه‌ای می‌گفت «جستجو بدون نتیجه».

هر تماس بیرونی باید پیام واقعی سرویس را لاگ کند. پیشوندها: `[tavily]`، `[wordpress]`، `[llm-timeout]`، `[researcher]`، `[brand-fix]`، `[timing]`، `[deadline]`، `[story-image]`، `[story-sticker]`، `[story-source-copy]`.

### ۷. سقف زمان روی فراخوانی مدل
یک تماس **۱۲۶٫۹ ثانیه** هنگ کرد و پاسخ خالی داد؛ تلاش بعدی در ۸٫۶ ثانیه جواب داد. آن یک هنگ کل اجرا را به ۲۹۳ ثانیه رساند.

`DEFAULT_TIMEOUT_MS = 45s`، `LONG_FORM_TIMEOUT_MS = 90s` (فقط نویسنده).

### ۸. قهرمان مخاطب است، نه برند
پرتکرارترین ایراد کیفی. در پنج اجرای پیاپی تکرار شد. هم در پرامپت نویسنده، هم در روبریک ویراستار، هم دو چک قطعی.

### ۹. `partialToRow` نام را با regex بازمی‌سازد
`supabase.ts` کلید snake_case را به camelCase برمی‌گرداند و با کلیدهای patch مقایسه می‌کند. اگر رفت‌وبرگشت دقیق نباشد، فیلد **بی‌صدا** از `updatePost` حذف می‌شود. `wpPostId ↔ wp_post_id` سالم است؛ نام‌های با رقم یا حروف بزرگ پیاپی نه.

### ۱۰. یکتاسازی URL باید `www.` را حذف کند
`gov.uk/x` و `www.gov.uk/x` دو کلید جدا می‌شدند و یک راهنما دو بار در منابع می‌آمد.

---

## واقعیت‌های تأییدشده (از حافظه ننویس، این‌ها آزمایش شده‌اند)

- **Tavily `include_domains` به هاست کامل نیاز دارد**: `["www.gov.uk"]` کار می‌کند، `["gov.uk"]` **بی‌صدا بی‌اثر** است.
- **Tavily `exclude_domains` قابل اعتماد نیست** — حذف یوتیوب، اینستاگرام و فیس‌بوک را جایگزین کرد. فیلتر حذف را سمت کد انجام بده.
- **وردپرس**: کاربر administrator با `publish_posts: true`. برای همین `createWordPressDraft` **پارامتر `status` نمی‌گیرد** و `"draft"` ثابت است. پارامتری‌اش نکن.
- **قالب وردپرس RTL نیست** (`lang="en-US"`). محتوا باید در `<div dir="rtl" lang="fa">` پیچیده شود.
- **رمز اپلیکیشن وردپرس فاصله دارد** و با و بدون فاصله هر دو کار می‌کند. فقط `trim` کن، فاصله‌های داخلی را دست نزن.
- **دسته‌های وردپرس**: `1` Uncategorized · `9` UK immigration · `27` Global Talent (فعلی).
- **ایتالیک در فارسی ممنوع** — حروف فارسی نسخه‌ی ایتالیک ندارند و مرورگر کجشان می‌کند. به‌جای `_…_` از `<p class="article-meta">` استفاده شده.

---

## امنیت

- **`isStudioAuthorized` در پروداکشن fail-closed است.** نبودِ `STUDIO_PASSWORD` یعنی بسته، نه باز. برنگردان.
- `middleware.ts` صفحه‌های `/studio` و `/blog` را با **Basic Auth** می‌بندد (مرورگر هدر سفارشی نمی‌فرستد). روی **Edge** اجرا می‌شود: `crypto` نودی در دسترس نیست، از Web Crypto استفاده کن.
- **matcher تست شده** (`curl -I` روی dev، مرداد ۱۴۰۵): `/studio/:path*` مسیر خالی `/studio` را **هم می‌گیرد**، پس `"/blog"` در فهرست matcher زائد است. نتیجه‌ی واقعی:

  | مسیر | کد |
  |---|---|
  | `/` · `/robots.txt` | ۲۰۰ (عمداً باز) |
  | `/studio` · `/blog` · `/blog/<slug>` | ۴۰۱ + `WWW-Authenticate` |
  | `/studio/` · `/blog/` | ۳۰۸ → ۴۰۱ |
  | `/api/*` | ۴۰۱ **بدون** `WWW-Authenticate` (کارِ `isStudioAuthorized`، تا پرامپت مرورگر باز نشود) |

  اگر خواستی `"/blog"` را حذف کنی، **دوباره همین تست را بگیر**؛ زائدبودنش به رفتار path-to-regexp نکست وابسته است، نه به قرارداد ما.
- `/` و `/robots.txt` عمداً باز.
- `.env.local` هرگز کامیت نشود. فایل بک‌آپ با پسوند دیگر (`.bak`) هم نسازی — `.gitignore` الگوی `.env*.local` دارد و `.env.local.bak` را پوشش نمی‌دهد.
- کرون غیرفعال است (`vercel.json` خالی). دستور برگرداندن در بالای `src/app/api/cron/weekly/route.ts`.

### bucket «social-assets» عمومی است — و این عمدی است

⚠️ **این را «امن» نکن.** اگر روزی دیدی یک bucket عمومی است و خواستی
`public = false` کنی، اول این را بخوان.

سه دلیل، هر سه هم‌زمان:

۱. **مسیرها UUID دارند.** حدس‌زدنی نیستند و جایی فهرست نمی‌شوند.
۲. **محتوا قرار است روی اینستاگرام عمومی منتشر شود.** چیزی که چند روز
   بعد برای همه دیده می‌شود، محرمانه نیست.
۳. **استودیو پشت Basic Auth است** (`middleware.ts`)، پس URL جایی بیرون
   از دستِ صاحب پروژه نمی‌رود.

در عوض چه چیزی به دست می‌آید: `getPublicUrl` یک الحاق رشته است — بدون
انقضا و بدون هیچ فراخوانی. لینک امضاشده انقضا دارد، پس نمی‌شود در
`image_paths` ذخیره‌اش کرد و باید هنگام هر نمایش دوباره ساخته شود.

اگر روزی محتوای واقعاً خصوصی در همان bucket نشست، تصمیم عوض می‌شود —
ولی آن‌وقت جایش `storage.ts` است، نه کل bucket.

**نکته‌ی SQL:** `insert … on conflict do nothing` روی bucketِ از قبل
موجود **هیچ کاری نمی‌کند** و public را عوض نمی‌کند. bucket این پروژه
با `public = false` ساخته شده بود و آن insert بی‌صدا از رویش رد می‌شد.
برای همین در `schema.sql` یک `update storage.buckets set public = true`
جداگانه هست.

---

## متغیرهای محیطی

**اجباری:** `OPENROUTER_API_KEY` · `STUDIO_PASSWORD` (در پروداکشن) · `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`

**اجباری فقط در پروداکشن:** `NEXT_PUBLIC_SITE_URL` — نبودش چیزی را نمی‌شکند، چون `siteUrl()` در `company.ts` به `http://localhost:3000` برمی‌گردد. یعنی روی سرور اگر جا بماند، **بی‌صدا** آدرس لوکال‌هاست تولید می‌شود.

**وردپرس:** `WORDPRESS_URL` · `WORDPRESS_USER` · `WORDPRESS_APP_PASSWORD` · `WORDPRESS_CATEGORY_ID` (اختیاری)

**اختیاری:** `TAVILY_API_KEY` · `PIPELINE_MODEL` · `WRITER_MODEL` · `OPENROUTER_BASE_URL` · `CRON_SECRET`

⚠️ **مدلِ واقعیِ در حال استفاده `google/gemini-3.7-flash` است**، از `PIPELINE_MODEL` در `.env.local`. پیش‌فرضِ داخل کد (`ai.ts` → `defaultModel()`) هنوز `google/gemini-2.5-flash` است و **کهنه** — اگر فقط کد را بخوانی، مدل غلط را گزارش می‌کنی. `WRITER_MODEL` ست نشده، پس نویسنده هم از همین استفاده می‌کند.

همه‌ی عددهای زمانی این فایل با همین مدل گرفته شده‌اند.

نبودِ هر اختیاری باید **بی‌صدا غیرفعال** کند، نه اینکه پایپ‌لاین را بشکند — الگوی Tavily.

---

## دیتابیس

`supabase/schema.sql` تنها مرجع است و **ابزار migration ندارد**. ستون جدید:
- بنر فاز جدید در **انتهای فایل** با `alter table … add column if not exists`
- بلوک `create table` موجود را دست نزن (روی دیتابیس زنده بی‌اثر است)
- **هر چهار mapper** باید `?? null` بگذارند برای ردیف‌های قدیمی: `postFromRow`، `socialPostFromRow`، `runFromRow`، `campaignFromRow` (`store/supabase.ts`)
- **کاربر باید SQL را دستی در Supabase اجرا کند** — یادآوری کن

---

## کار با این کاربر

- **کامیت نکن** مگر صریحاً بخواهد.
- بعد از هر بخش `npm run build`.
- **از حافظه ننویس.** وقتی درباره‌ی رفتار یک API بیرونی حرف می‌زنی، اول با یک فراخوانی واقعی تأیید کن. چند بار همین کار، فرض‌های غلط را قبل از پیاده‌سازی گرفته است.
- وقتی تشخیص چیزی قطعی نیست، بگو و به روبریک ویراستار بسپار — چک قطعی نساز.
- کاربر گاهی یک درخواست را با «قبل از اعمال نشانم بده» می‌دهد؛ جدی بگیر.
- تغییر روی `fuwment.com` (ساخت پیش‌نویس واقعی) بدون اجازه انجام نده. برای اندازه‌گیری، `WORDPRESS_URL=` را خالی کن.
