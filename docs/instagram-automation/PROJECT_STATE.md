# وضعیت واقعی پروژه — سیستم اینستاگرام فومنت

> این سند **تطبیق** `MASTER_PLAN.md` (نیت و طراحی) با **وضعیت واقعیِ ریپو** است.
> هر ادعا در این فایل مستقیماً از `git log`، `git status`، `git diff`، و خواندن
> فایل‌های واقعی تأیید شده — نه از حافظه یا گزارش نشست قبلی.
>
> تاریخ نگارش: ۲ شهریور ۱۴۰۵ (۲۰۲۶-۰۸-۲۴). شاخه: `instagram`.
>
> ## ⚠️ وضعیتِ نهایی — فازِ بصری ✅ COMPLETE (۲۰۲۶-۰۸-۲۵)
>
> **هرچه در ادامه‌ی این فایل «کامیت‌نشده»، «PARTIAL»، یا «منتظرِ اجرای هفتگیِ
> تأیید» گفته، مربوط به مراحلِ میانیِ همین فاز است و با بستنِ نهایی کهنه شده.**
> برای وضعیتِ واقعی و مرجع، بخشِ **«## فازِ بصری — بسته شد (نهایی، ۲۰۲۶-۰۸-۲۵)»**
> در انتهای همین فایل را بخوان — همه‌ی روایت‌های میانیِ زیر در آن جمع‌بندی و
> نهایی شده‌اند. بخش‌های تاریخی زیر برای ردگیریِ روندِ کار نگه داشته شده‌اند، نه
> به‌عنوانِ وضعیتِ فعلی.
>
> خلاصه‌ی یک‌خطی: اعتبارسنجیِ نهاییِ هفت‌پستی روی کدِ کامل (STYLE بریتانیاییِ V2 +
> هر دو اصلاحِ `imageSubject` + `sceneFamily`) اجرا شد، کاربر نتیجه را تأیید کرد،
> و این commit فاز را می‌بندد.
>
> ## ⚠️ آپدیت (۲۰۲۶-۰۸-۲۵): آزمایشِ مدل‌های تصویر هم بسته شد — ✅ APPROVED
>
> نتیجه در بخشِ «آزمایشِ مقایسه‌ی مدل‌های تصویر — نتیجه» (پایینِ فایل). تصمیمِ
> نهایی: `google/gemini-3.1-flash-lite-image` پیش‌فرضِ تولید می‌ماند، بدون
> تغییرِ کد، بدون لایه‌ی انتزاعِ ارائه‌دهنده. **کارِ بعدی تنوعِ چیدمان
> (Layout Variety) است** — نه فازِ استوری، نه رجیستریِ کلیدواژه.

---

## ۰. راستی‌آزمایی سریع (هرکس این فایل را خواند، اول این را بخواند)

⚠️ **کهنه — تصویرِ لحظه‌ای از میانه‌ی کار بود.** بعد از commit بستنِ تنوعِ
چیدمان (۲۰۲۶-۰۸-۲۷)، `git status --short` دوباره خالی است. برای وضعیتِ
واقعی به بخشِ پایانیِ فایل («تنوع چیدمان — بسته شد») مراجعه کن.

<details>
<summary>محتوای تاریخی (زمانِ نگارش: ۲۰۲۶-۰۸-۲۴)</summary>

```
git status --short        # ۶ فایل تغییرکرده + پوشه‌ی docs/ جدید — هیچ‌کدام کامیت نشده
git log --oneline -3       # e23d123 آخرین کامیت (تصویر AI کاور)، ۲ شهریور
```

**آخرین کامیت:** `e23d123 feat(slides): تصویر پس‌زمینه‌ی کاور با AI`

**در working tree، کامیت‌نشده:**
```
M CLAUDE.md
M src/lib/agents/instagram-orchestrator.ts
M src/lib/agents/instagram-writer.ts
M src/lib/agents/social-editor.ts
M src/lib/agents/weekly-grid.ts
M src/lib/agents/weekly-orchestrator.ts
?? docs/
```

این یعنی: **کار `sceneFamily` + معیار زندگی متن + به‌روزرسانی CLAUDE.md کامیت نشده‌اند.**
`npm run build` روی این وضعیت پاس شده (در همین نشست تأیید شد) ولی هنوز کامیت نخورده.

</details>

---

## ۱. جدول هشت‌فازی — وضعیت تأییدشده

| فاز | وضعیت | تأیید شد با |
|---|---|---|
| ۰ — پایه | ✅ **COMPLETE** | `schema.sql` خوانده شد |
| ۱ — انطباق سریع | ✅ **COMPLETE** | `types.ts` خوانده شد |
| ۱.۵ — چک برند انگلیسی | ✅ **COMPLETE** | `brand-checks.ts` قبلاً تأیید شده (این نشست دوباره نخواند) |
| ۲ — برنامه‌ریزی و دوزبانه‌سازی | ✅ **COMPLETE** | فایل‌ها و کامیت‌ها موجودند |
| ۳ — رندرکننده‌ی تصویر | ✅ **COMPLETE** | همه‌ی فایل‌ها موجود، کامیت `93890cb`/`5ad9b5e` |
| بصری | ✅ **COMPLETE** | اعتبارسنجیِ هفت‌پستیِ نهایی + commit بستن فاز — بخشِ پایانی |
| ۴ — استوری | ❌ **NOT STARTED** | هیچ فایل agent مربوطه پیدا نشد |
| ۵ — رجیستری کلیدواژه | ❌ **NOT STARTED** | `grep dm_keyword src/` صفر نتیجه در کد (فقط در schema) |

---

### فاز ۰ — پایه

**وضعیت: COMPLETE**

**نیت:** ساختار داده قبل از هر کد — جدول `content_weeks`، ستون‌های `language`/`dm_keyword`/`image_paths`/`rendered_at`، constraint فرمت `story`، bucket عمومی.

**تأیید شد در `supabase/schema.sql`:**
- خط ۲۰۱: `check (format in ('carousel','post','reels','story'))`
- خط ۲۰۶: شکل داده‌ی `story` (۱ تا ۳ اسلاید) در همان constraint
- خط ۲۲۴-۲۲۶: `dm_keyword` + ایندکس یکتای شرطی
- خط ۲۲۹: `image_paths jsonb not null default '[]'`
- خط ۲۴۴: `create table if not exists content_weeks`
- خط ۲۶۳-۲۶۴: `social_posts.week_id` + ایندکس
- خط ۳۱۶: `rendered_at timestamptz`
- bucket عمومی: `insert ... on conflict do nothing` + `update storage.buckets set public = true` جداگانه (چون insert روی bucket موجود بی‌اثر بود)

**فایل‌های کلیدی:** `supabase/schema.sql` (تنها مرجع — بدون ابزار migration؛ کاربر SQL را دستی در Supabase اجرا می‌کند)

**چیزی کم نیست.**

---

### فاز ۱ — انطباق سریع برند

**وضعیت: COMPLETE**

**نیت:** خروجی موجود را بدون کار ساختاری با برندگاید منطبق کند.

**تأیید شد در `src/lib/agents/types.ts`:**
- `hashtags: z.array(z.string()).min(3).max(5)` روی `InstagramCarouselSchema` (خط ۳۹۲) — کامنت صریح می‌گوید بازه‌ی قبلی `min(8).max(15)` بود
- ابعاد ۴:۵ در `CarouselPreview.tsx` (این نشست دوباره نخواند، ولی در تاریخچه‌ی گفت‌وگو با کامیت `3872aa1` مستند است)

**فایل‌های کلیدی:** `src/lib/agents/types.ts`، `src/lib/agents/social-checks.ts`، `src/components/studio/CarouselPreview.tsx`

**چیزی کم نیست.**

---

### فاز ۱.۵ — لایه‌ی چک برند انگلیسی

**وضعیت: COMPLETE**

**نیت:** گارد حقوقی روی محتوای انگلیسی — عناوین حفاظت‌شده، ادعای اقتدار کاذب.

⚠️ **این نشست خودِ `brand-checks.ts` را دوباره نخواند** (فایل بزرگ و پرخطر — قاعده‌ی «فایل‌های ناحیه‌ی بلاگ باز نمی‌شوند» مستقیماً شاملش نمی‌شود چون این فایل مشترک است، ولی برای صرفه‌جویی در بودجه‌ی این نشست باز نشد). ادعای COMPLETE بر پایه‌ی CLAUDE.md بخش ۵ (رول‌های سختِ پروژه، که خودش محصول همین فاز است و در این نشست خوانده شد) و کامیت‌های تاریخچه است.

**اگر نشست بعدی نیاز به تغییر این فاز داشت، اول `brand-checks.ts` را بخواند و رأساً تأیید کند** — این‌جا فقط از روی شواهد غیرمستقیم COMPLETE علامت خورده.

**فایل‌های کلیدی:** `src/lib/agents/brand-checks.ts`، `src/lib/agents/social-checks.ts` (`checkLanguageMatch`)

---

### فاز ۲ — برنامه‌ریزی و دوزبانه‌سازی

**وضعیت: COMPLETE**

**نیت:** از اجرای تکی به هفته‌ی کامل — برنامه‌ریز هفتگی، `route`/`language` در بریف، کپی‌رایتر و ویراستار دوزبانه.

**تأیید شد — همه‌ی فایل‌ها موجودند:**
- `src/lib/agents/weekly-grid.ts` — `WEEKLY_GRID` (۷ اسلات ثابت: روز، زبان، مسیر، گروه مخاطب، نوع محتوا)
- `src/lib/agents/weekly-planner.ts` — `planWeek`، `RECENT_TITLES_DAYS = 90`، `MIN_DISTINCT_JOURNEY_STAGES = 4`
- `src/lib/agents/weekly-orchestrator.ts` — `runWeek`، تفکیک از `planWeek`
- `src/app/api/social/weekly/route.ts` — مسیر API
- `src/components/studio/WeeklyPanel.tsx` — پنل استودیو (⚠️ نامش `WeeklyPanel`، نه `WeekPanel` — اگر جایی به نام دیگر ارجاع داده شده اشتباه است)
- `instagram-writer.ts`: `FA_BLOCK`/`EN_BLOCK` دوزبانه (تأیید مستقیم — همین نشست خوانده و ویرایش کرد)
- `social-editor.ts`: شاخه‌ی `input.brief.language === "en"` (تأیید مستقیم)

**اعتبارسنجی واقعی که در همین نشست انجام شد:** یک اجرای هفتگی کامل روی `2026-08-22` — Supabase مستقیماً کوئری شد: `content_weeks` یک ردیف با `status=done` و `run_ids` به طول ۷ دارد.

**چیزی کم نیست.**

---

### فاز ۳ — رندرکننده‌ی تصویر

**وضعیت: COMPLETE**

**نیت:** PNG آماده‌ی انتشار، نه فقط متن.

**تأیید شد — همه‌ی فایل‌ها موجودند و در این نشست خوانده شدند:**
- `src/lib/slide-spec.ts` — منبع مشترک چیدمان (بدون `server-only`، عمداً)
- `src/lib/slide-renderer.ts` — `renderSlide`/`renderCarousel`، async (به‌خاطر `loadImage`)
- `src/lib/storage.ts` — `renderSlidesForPost`، خارج از `BlogStore`
- `src/app/api/social/posts/[id]/render/route.ts` — مسیر رندر دوباره
- `src/components/studio/SlideImages.tsx` — نمایش PNG واقعی با `?v={renderedAt}`
- `next.config.mjs`: `experimental.serverComponentsExternalPackages: ["@napi-rs/canvas"]` — تأیید شد
- فونت‌ها: پنج فایل Vazirmatn woff2 + `OFL.txt` در `public/fonts/` — همه موجودند (`find` تأیید کرد)
- `package.json`: `@napi-rs/canvas` نصب است؛ **Playwright غایب است** (قاعده‌ی ایمنی رعایت شده)

**چیزی کم نیست.**

---

### فاز بصری

**وضعیت: ✅ COMPLETE — کامیت شده، با اجرای هفتگیِ واقعی تأیید شده.**
(شرحِ زیر مربوط به مرحله‌ی میانیِ کار است؛ نتیجه‌ی نهایی در بخشِ پایانیِ فایل.)

**نیت:** جذابیت بصری بدون از دست دادن اعتبار — حذف نوار تأکید، کیکر داخل بلوک، مرکز نوری، تصویر AI، زندگی متن، تنوع صحنه.

**آنچه کامیت شده (`e23d123` و قبل‌تر):**
- حذف نوار تأکید، کیکر داخل بلوک، مرکز نوری ۴۵/۵۵ (`e288f6c`, `8b51bd8`)
- `src/lib/image-gen.ts` — OpenRouter، مدل `google/gemini-3.1-flash-lite-image`، `STYLE` قفل‌شده
- `IMAGE_SCRIM` در `slide-spec.ts` — `alpha: 0.75`, `bodyOpacity: 1.0`
- `imageSubject` اختیاری در `SlideSchema` (`types.ts`) و `Slide` (`store/types.ts`)
- کش تصویر پس‌زمینه در `storage.ts` (`coverImagePath`, دانلود قبل از تولید)

**آنچه در working tree هست، کامیت‌نشده:**
- **معیار زندگی متن** در `social-editor.ts`: `LIVENESS_CRITERION` دوزبانه با لنگر نمره؛ `brandVoice` صریحاً از این محور جدا شد
- **قاعده‌ی ساختار جمله** در `instagram-writer.ts`: سه مثال قبل/بعد در `FA_BLOCK`، نسخه‌ی فشرده در `EN_BLOCK` — هر سه مثال از خروجی واقعی همین ریپو
- **آزمون تمایز `imageSubject`** در `instagram-writer.ts`: «اگر صحنه برای هر موضوعی جواب می‌دهد، رد است» + چهار مثال از دنیاهای متفاوت + ممنوعیت نمایشگر روشن
- **`sceneFamily`** در `weekly-grid.ts`: نوع `VisualGroup` (شش گروه: `surface`/`interior`/`architecture`/`threshold`/`nature`/`object`)، شش استخر (`SCENE_POOLS`)، `weekIndexOf`، `sceneFamilyFor`، `assertSceneVariety` (اجرا در بارگذاری ماژول)
- پلامبینگ: `sceneFamily` از `WEEKLY_GRID` → `runWeek` → `runInstagramPipeline` → `runInstagramWriter`/`runInstagramRevision`
- `CLAUDE.md`: جدول زمان‌بندی با تفکیک دو اجرای واقعی، نتیجه‌گیری «موازی راحت جا می‌شود، ترتیبی دیگر نه» جایگزین نسخه‌ی قدیمی

**اعتبارسنجی‌های واقعی که در این نشست انجام شد:**
1. معیار زندگی متن: دو اجرای Instagram با موضوع global-talent — `persian` این‌بار ۶ و ۸ گرفت (قبلاً تقریباً همیشه ۸/۹ بود)، و `brandVoice` جدا ماند روی ۹
2. `imageSubject` قبل از اصلاح تمایز: هر دو اجرا «لپ‌تاپ روی میز کنار قهوه» دادند — مشکل با داده واقعی تأیید شد
3. بعد از اصلاح مثال‌ها: دو اجرا روی دو مسیر متفاوت (`global-talent`/`innovator-founder`) — صحنه‌ها فرق کردند («تخته‌شاسی چوبی» / «وایت‌برد اتاق جلسه»)، ولی صحنه‌ی دوم عیناً یکی از مثال‌های پرامپت بود — **تقلید از مثال هنوز کاملاً حل نشد**
4. `assertSceneVariety`: در بارگذاری اول یک نقض واقعی گرفت (سه‌بار `interior` در یک هفته با جدول دست‌ساز)؛ بعد از جستجوی کامل روی ۳۳۳٬۰۰۲ ترکیب، جدول معتبر پیدا و نصب شد؛ چهار هفته با `node` اجرا و چاپ شد — بدون نقض

**آنچه در آن لحظه هنوز انجام نشده بود (⚠️ کهنه — هر دو مورد اکنون انجام شده‌اند):**
- ~~اجرای هفتگی واقعی (نه اجراهای تکی) با کد فعلی `sceneFamily`~~ ✅ انجام شد
  دو بار: یک اعتبارسنجیِ اول (که یک نشتِ معماریِ باقی‌مانده را کشف کرد)، و
  اعتبارسنجیِ نهایی بعد از اصلاحِ آن — نتیجه در بخشِ پایانیِ فایل.
- ~~کامیت خودِ کار~~ ✅ با `feat(instagram): complete visual phase` کامیت شد.

**فایل‌های کلیدی:** `src/lib/agents/weekly-grid.ts`، `src/lib/agents/instagram-writer.ts`، `src/lib/agents/social-editor.ts`، `src/lib/agents/instagram-orchestrator.ts`، `src/lib/agents/weekly-orchestrator.ts`، `src/lib/image-gen.ts`، `src/lib/slide-spec.ts`

**وابستگی:** فاز ۴ (استوری) به این فاز وابسته است چون از همان `slide-spec`/رندرکننده استفاده می‌کند.

---

### فاز ۴ — استوری

**وضعیت: ❌ NOT STARTED**

**نیت:** ۲ تا ۳ استوری در روز، یک ارکستریتور و سه خروجی.

**تأیید شد:** هیچ فایل agent مربوط به استوری در `src/lib/agents/` پیدا نشد. تنها اثر موجود، constraint دیتابیس در فاز ۰ است (`format = 'story'`، شکل ۱ تا ۳ اسلاید) — که پیش‌نیاز است، نه پیاده‌سازی.

**طراحی موجود (در `MASTER_PLAN.md`، پیاده‌نشده):** رندر ۱۰۸۰×۱۹۲۰ با حاشیه‌ی امن ۲۵۰ بالا/پایین؛ متن استیکر در فیلد جداگانه (نه پخته در تصویر)؛ `FORMAT_META` تایپ‌شده به‌جای ternary در `SocialPostCard:82`.

**وابستگی:** به فاز بصری و به تنوع چیدمان (بخش ۱۷ همین سند) — چون استوری همان `slideText()`/`slide-spec` را لازم دارد. **هر دو اکنون COMPLETE‌اند** (۲۰۲۶-۰۸-۲۵ و ۲۰۲۶-۰۸-۲۷)، پس این وابستگی برآورده شده.

---

### فاز ۵ — رجیستری کلیدواژه‌ی دایرکت

**وضعیت: ❌ NOT STARTED**

**نیت:** CTA دایرکت قابل مدیریت — کلیدواژه‌ی یکتا per post با متن پاسخ آماده.

**تأیید شد:** `grep -rl "dm_keyword|dmKeyword" src/` صفر نتیجه داد. ستون و ایندکس یکتا در schema هست (فاز ۰) ولی هیچ کد مصرف‌کننده‌ای نوشته نشده.

**وابستگی:** مستقل — هر وقت قابل انجام است، به فازهای دیگر گره نخورده.

---

## ۲. فاز و تسکِ دقیقِ فعلی

⚠️ **کهنه.** فازِ بصری، آزمایشِ مقایسه‌ی مدل‌های تصویر، و تنوعِ چیدمان — هر سه
اکنون COMPLETE و کامیت‌شده‌اند — بخش «فاز بصری، مرحله‌ی نهایی» زیر توصیفِ یک
نقطه‌ی میانی از فرایند است، نه وضعیتِ فعلی. تسکِ فعلی: **فاز ۴ — استوری**،
تا کاربر صریحاً نگفته شروعش کن، شروع نشود. بخشِ پایانیِ فایل («تنوع چیدمان —
بسته شد») را بخوان.

<details><summary>محتوای تاریخی</summary>

**فاز بصری، مرحله‌ی نهایی: اجرای هفتگی واقعی گرفته شد (بخش انتهای فایل).**
کد کامیت شده (`f0d41b1`). یک یافته‌ی جدید و نسبتاً مهم به‌دست آمد که باید قبل
از تصمیم «همین الان کامیت/تأیید نهایی» به کاربر گزارش شود — نگاه کنید به بخش
«اجرای هفتگی واقعی — نتیجه».

</details>

---

## ۳. نقطه‌ی دقیقِ توقف پیاده‌سازی

⚠️ **کهنه — لحظه‌ای از میانه‌ی کار.** برای نقطه‌ی توقفِ واقعی (فازِ بصری،
آزمایشِ مدل‌های تصویر، و تنوعِ چیدمان هر سه بسته شده‌اند؛ کارِ بعدی فاز ۴ —
استوری است) به بخشِ پایانیِ فایل مراجعه کن.

<details><summary>محتوای تاریخی</summary>

آخرین عمل انجام‌شده در این نشست: چهار اجرای تکیِ `POST /api/social/instagram` (نه اجرای هفتگی) برای آزمودن معیار زندگی متن و آزمون تمایز `imageSubject` — هر چهار موفق، نتایج در بخش «فاز بصری» بالا ثبت شده. بعد از آن، این سند شروع به نوشتن شد و **هیچ پیاده‌سازی جدیدی انجام نگرفت.**

آخرین سؤال باز از طرف دستیار (بی‌پاسخ‌مانده): آیا اجرای هفتگی واقعی (~۶ دقیقه، ~$۰.۲۴) گرفته شود؟

</details>

---

## ۴. کار کامیل‌نشده‌ی فعلی

⚠️ **کهنه.** همه‌ی فایل‌های زیر اکنون کامیت شده‌اند (به‌همراهِ اصلاح‌های بعدیِ
`instagram-writer.ts` و `image-gen.ts`). `git status --short` فعلی خالی است.

<details><summary>محتوای تاریخی</summary>

دقیقاً همان چیزی که `git status --short` نشان می‌دهد (بخش ۰ همین سند). خلاصه:

| فایل | چه چیزی عوض شده |
|---|---|
| `CLAUDE.md` | جدول زمان‌بندی با عدد رندر و تصویر |
| `instagram-orchestrator.ts` | پارامتر `sceneFamily?` + تزریق به `write`/`revise` |
| `instagram-writer.ts` | `sceneBlock()`، آزمون تمایز، سه مثال ساختار جمله |
| `social-editor.ts` | `LIVENESS_CRITERION`، جداسازی `brandVoice`/`persian` |
| `weekly-grid.ts` | `VisualGroup`، `SCENE_POOLS`، `sceneFamilyFor`، `assertSceneVariety` |
| `weekly-orchestrator.ts` | فراخوانی `sceneFamilyFor` و پاس‌دادنش |

هر شش فایل با `npm run build` در همین نشست تست شدند (پاس شد). **کامیت نشدند چون کاربر هنوز دستور کامیت نداده و نتیجه‌ی اجرای هفتگی دیده نشده.**

</details>

---

## ۵. تصمیم‌های معماری مهمِ گرفته‌شده در این پیاده‌سازی

(این‌ها را `MASTER_PLAN.md` بخش ۱۰ و ۱۱ هم دارد؛ اینجا تکرار می‌شود تا این سند به‌تنهایی هم کافی باشد.)

- **رندرکننده `@napi-rs/canvas` است، نه satori یا Playwright.** satori با تست رد شد: ترتیب کلمات فارسی در خروجی واقعی برعکس شد (بدون الگوریتم Bidi). Playwright هرگز به `package.json` اضافه نشد — قاعده‌ی ایمنی صریح.
- **`slide-spec.ts` منبع واحد چیدمان است**، هم برای پیش‌نمایش (`CarouselPreview.tsx`) هم رندرکننده. هیچ عدد چیدمانی در جای دیگر تکرار نمی‌شود.
- **bucket `social-assets` عمداً عمومی است.** دلیل کامل در `CLAUDE.md` بخش امنیت: مسیرها UUID دارند، محتوا قرار است روی اینستاگرام عمومی منتشر شود، و استودیو پشت Basic Auth است.
- **تصویر AI فقط برای اسلاید کاور.** بقیه‌ی اسلایدها پس‌زمینه‌ی سرمه‌ای ثابت می‌گیرند — تنها اسلایدی که در گرید و فید دیده می‌شود کاور است؛ تصویر برای همه یعنی هفت برابر هزینه برای چیزی که اکثر بیننده‌ها نمی‌بینند.
- **`IMAGE_SCRIM.alpha = 0.75` و `bodyOpacity = 1.0` از محاسبه‌ی WCAG آمده، نه سلیقه.** `0.70` کنتراست ۴.۲۵ می‌دهد و AA (۴.۵) را رد می‌کند. متن بدنه با `opacity: 0.8` در **هیچ** آلفایی به AA نمی‌رسد؛ برای همین `bodyOpacity` جدا از آلفا لازم بود.
- **یک منتقد در سطح هفته، نه هفت.** `saveLessons` قفل ندارد (read-modify-write بدون تراکنش) و `MAX_ACTIVE_LESSONS_PER_AGENT = 8` — هفت منتقد هم‌زمان حافظه‌ی خودبهبودی چهار ایجنت را با یک بچ پاک می‌کردند.
- **ایده‌یاب در مسیر هفتگی دور زده می‌شود.** تابع امتیازدهی‌اش صریحاً روی «حرف تازه‌تر» و «قلاب قوی‌تر» پاداش می‌دهد که هر دو علیه وفاداری به موضوعِ ازپیش‌تعیین‌شده کار می‌کنند.
- **`sceneFamily` در سطح هفته تصمیم‌گیری می‌شود، نه توسط کپی‌رایتر یا برنامه‌ریز.** برنامه‌ریز موقع تصمیم متن کاروسل را ندیده، پس صحنه‌ی دقیق را نمی‌تواند بسازد؛ کپی‌رایتر محلی است و هفت اجرای موازی همدیگر را نمی‌بینند. «خانواده» تنها لایه‌ای است که هم اطلاعات کافی دارد هم دید کل هفته.
- **معیار ششم روبریک ساخته نشد.** `score = مجموع پنج معیار × ۲` یک فرض ساختاری است که در چهار ارکستریتور و `SocialReviewSchema` تکرار شده؛ اضافه‌کردن معیار ششم ضریب را می‌شکند و `score` را بالای ۱۰۰ می‌برد که schema رد می‌کند و کل اجرا `throw` می‌خورد. به‌جایش معیار `persian` موجود صریح تعریف شد.
- **`brandVoice` و «زنده‌بودن متن» صریحاً دو محور جدا شدند.** قبلاً `persian` این دو را قاطی می‌کرد و تقریباً همیشه ۸ یا ۹ می‌گرفت — معیاری که به همه‌چیز نمره‌ی بالا می‌دهد معیار نیست.

---

## ۶. تصمیم‌های فقط-در-گفت‌وگو (در کد آشکار نیستند)

این‌ها را کد به‌تنهایی نشان نمی‌دهد؛ نشست بعدی باید از این‌جا بخواندشان:

- **مثال ✅ به همان اندازه‌ی مثال ❌ تقلید می‌شود.** نسخه‌ی اول قاعده‌ی `imageSubject` دو مثال ✅ داد که هر دو صحنه‌ی میز کار بودند؛ مدل در دو اجرای واقعی دقیقاً همان را برگرداند. این یک یافته‌ی تجربی است، نه فرض — و بعد از اصلاح (چهار مثال از دنیاهای متفاوت) هم یکی از دو اجرای بعدی عیناً یکی از مثال‌های جدید را برگرداند. **مسئله کم شد، حذف نشد.**
- **چرا `sceneFamily` در سطح هفته و نه در سطح اجرای تکی:** استدلال قاطع، دلیل سوم بود که کاربر خودش تأیید کرد — برنامه‌ریز موقع تصمیم متن را ندیده، پس «خانواده» همان مقدار اطلاعاتی است که پشتوانه دارد. اگر روزی وسوسه شدی این را به کپی‌رایتر بسپاری، این استدلال را دوباره بخوان.
- **چرا شش گروه بصری (نه پنج):** با پنج گروه، ساختار `WEEKLY_GRID` (روزهای ۰/۳/۶ همگی `w % 3` و روزهای ۱/۴ همگی `(w+1) % 3` می‌گیرند) هیچ ترتیبی از ۱۸۶٬۶۲۴ ترکیب را برآورده نکرد. گروه ششم (`threshold`) اضافه شد و اولین جواب در ۳۳۳٬۰۰۲ ترکیب پیدا شد. **این یک محدودیت ریاضیِ اثبات‌شده است، نه سلیقه** — اگر کسی گروه‌ها را کم کرد، باید دوباره جستجو بگیرد یا `assertSceneVariety` شکست می‌خورد.
- **چرا `MAX_SURFACE = 1` و بقیه `MAX_PER_GROUP = 2`:** با هفت روز و شش گروه، اصل لانه‌کبوتری «همه متمایز» را غیرممکن می‌کند — پس سقف ۲ برای اکثر گروه‌ها سخت‌گیرترین قید ممکن است. `surface` («میز و کاغذ») سقف جداگانه‌ی ۱ گرفت چون **دقیقاً همان الگویی بود که کل مسئله را نشان داد** («لپ‌تاپ روی میز» در دو اجرای پشت‌سرهم پیش از اصلاح).
- **چرا `weekIndexOf` از رشته‌ی `week_start` حساب می‌شود، نه از `Date.now()`:** تا رندر دوباره‌ی یک هفته‌ی قدیمی صحنه‌ی متفاوتی نگیرد — نتیجه نباید به «الان» وابسته باشد، دقیقاً همان استدلال منطقه‌ی زمانی صریح در `week.ts`.
- **دو دلیل فنی برای `LIVENESS_CRITERION` روی معیار `persian`، نه معیار جدید:** (۱) `score = Σ×2` در چهار ارکستریتور و schema تکرار شده و شکستنش یعنی مرگ کل اجرا با schema violation، (۲) `persian` از قبل مالک این محور بود («بدون جمله‌های ماشینی») ولی بی‌تعریف بود — ۲۴ از ۲۶ بازبینی واقعی ۸ یا ۹ گرفته بودند، یعنی معیار موجود از قبل کور بود، نه نبود.
- **لنگر نمره‌ی `persian` عمداً «۶» است، نه چیزی که خودکار `revise` بسازد.** با چهار معیار دیگر روی ۹، امتیاز ۸۴ می‌شود — بالای حد نصاب ۷۰. طراحی عمدی است: معیار **گزارش می‌دهد**، بازنویسی خودکار نمی‌سازد؛ چون تطبیق الگو روی فارسی («اسم مصدر») خودش دقیقاً همان تله‌ی «چک قطعی غلط» است که در CLAUDE.md مستند شده.
- **چرا سه مثال ساختار جمله همگی از خروجی واقعیِ همین ریپو گرفته شدند، نه ساختگی:** یک تصمیم صریح کاربر بود. مثال ساختگی به مدل یاد می‌دهد از چیزی پرهیز کند که خودش هرگز نمی‌نویسد؛ مثال واقعی نشان می‌دهد دقیقاً کجا این مدل خودش لغزیده.

---

## ۷. فایل‌های مهم و مسئولیتشان

| فایل | مسئولیت |
|---|---|
| `src/lib/agents/weekly-grid.ts` | پیکربندی ثابت هفته: `WEEKLY_GRID` (زبان/مسیر/مخاطب/نوع محتوا per day)، `SCENE_POOLS`، `sceneFamilyFor`، `assertSceneVariety` |
| `src/lib/agents/weekly-planner.ts` | `planWeek` — یک فراخوانی مدل، ۷ اسلات (`topic`/`hook`/`painPoint`/`journeyStage`) |
| `src/lib/agents/weekly-orchestrator.ts` | `runWeek` — ۷ اجرای موازی + یک منتقد سطح‌هفته |
| `src/lib/agents/instagram-orchestrator.ts` | `runInstagramPipeline` — زنجیره‌ی کامل یک کاروسل، شامل گام `slide-render` |
| `src/lib/agents/instagram-writer.ts` | کپی‌رایتر + بازنویسی، دوزبانه، قاعده‌ی زندگی متن، `imageSubject` |
| `src/lib/agents/social-editor.ts` | ویراستار اجتماعی — روبریک پنج‌معیاره، `LIVENESS_CRITERION` |
| `src/lib/agents/social-loop.ts` | حلقه‌ی مشترک نویسنده⇄ویراستار برای چهار پایپ‌لاین اجتماعی |
| `src/lib/agents/types.ts` | همه‌ی Zod schemaها — `SlideSchema`، `InstagramCarouselSchema`، `SocialReviewSchema`، `WeeklySlotSchema` |
| `src/lib/slide-spec.ts` | منبع واحد چیدمان (ابعاد، رنگ، تایپوگرافی، `IMAGE_SCRIM`) — بدون `server-only` |
| `src/lib/slide-renderer.ts` | JSON → PNG خالص، با `@napi-rs/canvas` |
| `src/lib/storage.ts` | آپلود/دانلود Supabase Storage، `renderSlidesForPost` |
| `src/lib/image-gen.ts` | تولید تصویر پس‌زمینه با OpenRouter |
| `src/lib/week.ts` | محاسبه‌ی مرز هفته (شنبه، `Asia/Tehran`) |
| `src/components/studio/WeeklyPanel.tsx` | UI برنامه‌ریزی و اجرای هفته |
| `src/components/studio/SlideImages.tsx` | نمایش PNG واقعی با fallback به `CarouselPreview` |
| `src/app/api/social/weekly/route.ts` | مسیر API برنامه‌ریز هفتگی |
| `src/app/api/social/posts/[id]/render/route.ts` | مسیر رندر دوباره |
| `CLAUDE.md` | مرجع پروژه — قواعد سخت، زمان‌بندی، امنیت. **باید با هر تغییر معماری به‌روز بماند.** |

---

## ۸. تصمیم‌های دیتابیس/schema

- `supabase/schema.sql` تنها مرجع است، **بدون ابزار migration.** ستون جدید همیشه در انتهای فایل با `alter table ... add column if not exists`.
- **کاربر باید SQL را دستی در Supabase اجرا کند** — این پروژه مکانیزم اجرای خودکار migration ندارد.
- هر چهار mapper (`postFromRow`, `socialPostFromRow`, `runFromRow`, `campaignFromRow` در `store/supabase.ts`) باید `?? null` بگذارند برای ردیف‌های قدیمی که ستون جدید ندارند.
- `content_weeks`: جدول جدید، هشت ستون، مستقل از `pipeline_runs`. هفت فرزندِ اجرای هفتگی همچنان `kind: "instagram"` در `pipeline_runs` می‌گیرند — `RunKind` هرگز `"weekly"` نشد. الگوی دقیقاً مثل `campaign-orchestrator`.
- `social_posts.week_id`: nullable، فقط اجراهای هفتگی پرش می‌کنند. اجراهای تکی (مسیر مستقل استودیو) `null` می‌گذارند.
- `dm_keyword`: ستون و ایندکس یکتا از فاز ۰ موجودند؛ **هیچ کد مصرف‌کننده‌ای نوشته نشده** (فاز ۵ شروع نشده).
- **⚠️ Bucket `social-assets` عمداً `public = true` است.** اگر کسی روزی خواست «امنش کند»، اول دلیلش را در `CLAUDE.md` بخش امنیت بخواند — سه دلیل مستقل آنجا مستند است.

---

## ۹. تصمیم‌های API/یکپارچه‌سازی

- **تولید متن:** OpenRouter، مدل `google/gemini-3.7-flash` (از `PIPELINE_MODEL` در `.env.local` — پیش‌فرض کدِ `ai.ts` هنوز `gemini-2.5-flash` و کهنه است).
- **تولید تصویر:** همان OpenRouter، `/api/v1/images`، مدل `google/gemini-3.1-flash-lite-image`. همان `OPENROUTER_API_KEY` — **صفر متغیر محیطی جدید**. قیمت با نرخ رسمی گوگل تطبیق دارد، بدون مارک‌آپ (به‌صورت تجربی با سه فراخوانی واقعی تأیید شد: میانگین $0.0339).
- **`aspect_ratio: "4:5"`** درخواست می‌شود ولی مدل فقط `1K` می‌دهد (~۹۰۰×۱۱۲۰ برای بومِ ۱۰۸۰×۱۳۵۰) — حدود ۲۰٪ بزرگ‌نمایی زیر لایه‌ی تیره، عمداً پذیرفته شده.
- **retry:** `runAgentJSON` در `ai.ts` دو تلاش با بازخورد خطای تلاش قبلی. **هیچ backoff برای ۴۲۹ ندارد** — سه تلاش فوری بدون تأخیر (بدهی فنی ثبت‌شده، غیربحرانی).
- **انتشار اینستاگرام دستی است.** بدون Instagram Graph API در هیچ فازی — این یک تصمیم محصولی صریح است، نه محدودیت فنی موقت.
- **وردپرس بی‌ربط به این مسیر است** — پایپ‌لاین اینستاگرام هرگز `wordpress.ts` را صدا نمی‌زند.

---

## ۱۰. الزامات محیط/پیکربندی

اجباری (از `.env.local`، همه تأیید شدند که مقدار دارند):
- `OPENROUTER_API_KEY`
- `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`
- `STUDIO_PASSWORD` (در پروداکشن؛ در dev نبودش یعنی باز)

مرتبط با این پروژه (اختیاری):
- `PIPELINE_MODEL` — ست شده به `google/gemini-3.7-flash`؛ بدون این، `ai.ts` پیش‌فرض کهنه استفاده می‌کند
- `TAVILY_API_KEY` — بی‌ربط به مسیر اینستاگرام (فقط پایپ‌لاین بلاگ پژوهشگر دارد)

**بدون کلید جدید برای هیچ کاری در این پروژه** — تولید تصویر هم از همان `OPENROUTER_API_KEY` استفاده می‌کند.

---

## ۱۱. باگ‌های شناخته‌شده و مسائل حل‌نشده

### بحرانی برای ادامه‌ی کار (باید قبل از فاز ۴/۵ دید)
- **اجرای هفتگی با کد فعلی `sceneFamily` هنوز گرفته نشده.** ریسک واقعی: قفل `assertSceneVariety` روی جدول *ثابت* درست است، ولی رفتار مدل زیر فشار هفت اجرای موازی واقعی (نه چهار اجرای تکی که در این نشست گرفته شد) تأیید نشده.
- **تقلید از مثال حل نشده.** حتی با چهار مثال متفاوت، مدل می‌تواند عیناً یکی را کپی کند. اگر بعد از اجرای هفتگی واقعی همچنان دیده شد، راه‌حل باید در سطح خانواده باشد (مثلاً چند صحنه‌ی نمونه‌ی متفاوت per خانواده، نه یکی).

### غیربحرانی (ثبت‌شده در `MASTER_PLAN.md` بخش ۹، این نشست دوباره تأیید نکرد مگر خلافش گفته شود)
- `checkBrandAsSubject` مسیر انگلیسی را نمی‌گیرد (`BRAND_SUBJECT_STARTERS` مشترک با بلاگ است؛ راه امن ثابت جدا برای انگلیسی، نه ویرایش این یکی)
- چهار سقف `.transform` مرده باقی مانده — **این نشست دو مورد را مستقیماً در `types.ts` تأیید کرد:** `SeoOutput.metaTitle`/`metaDescription` (خط ۱۸۰-۱۸۱) و `InstagramCarousel.caption` (خط ۳۸۹؛ کف `min(80)` تقریباً هرگز به سقف `2200` نمی‌رسد). `ReelsScript.onScreenText` هم در فهرست `MASTER_PLAN` است، این نشست جداگانه تأیید نکرد.
- `campaign-orchestrator` همان مسابقه‌ی `saveLessons` را دارد (شدت کمتر — بحرانی نیست)
- `ai.ts` بدون backoff برای ۴۲۹
- ایده‌یاب در مسیر کمپین `route` نمی‌گیرد
- دکمه‌ی «تأیید برای انتشار» وقتی چک مسدودکننده رد شده غیرفعال نمی‌شود
- پنج ردیف قدیمیِ انگلیسی در `social_posts` ستون `language = 'fa'` دارند (پیش از کامیت `2ee16ad` — داده‌ی مرده، نه باگ زنده؛ این نشست هم دوباره در کوئری‌های ابتدایی همین را دید)
- تله‌ی ارتقای Next 15: `experimental.serverComponentsExternalPackages` در Next 15 به `serverExternalPackages` تغییر نام می‌دهد — اگر منتقل نشود فقط هشدار می‌دهد و بیلد با خطای native module می‌شکند

---

## ۱۲. تست‌های انجام‌شده و نتیجه‌شان

| تست | نتیجه |
|---|---|
| `npm run build` روی working tree فعلی (شش فایل تغییرکرده) | ✅ پاس — بدون خطای typecheck/lint |
| اجرای هفتگی کامل (`2026-08-22`) | ✅ ۷/۷ موفق — از Supabase تأیید شد (`content_weeks.status = done`, `run_ids.length = 7`) |
| اجرای انتها-به-انتها با رندر تصویر (۲ اجرای فارسی) | ✅ ۴۶.۵ و ۵۰.۶ ثانیه؛ تفکیک کامل گام‌به‌گام در CLAUDE.md ثبت شده |
| سه تولید تصویر AI واقعی | ✅ میانگین $0.0339، ۲.۸–۴.۱ ثانیه هرکدام |
| معیار زندگی متن — دو اجرا | ✅ `persian` این‌بار ۶ و ۸ (قبلاً تقریباً همیشه ۸/۹) |
| آزمون تمایز `imageSubject` — قبل از اصلاح | ❌ (به‌معنای مثبت) هر دو اجرا «لپ‌تاپ روی میز» دادند — مشکل با داده تأیید شد |
| آزمون تمایز — بعد از اصلاح، دو مسیر متفاوت | 🟡 صحنه‌ها فرق کردند ولی یکی عیناً کپیِ یک مثال پرامپت بود |
| `assertSceneVariety` روی جدول اول (دست‌ساز) | ❌ شکست — سه‌بار `interior` در هفته‌ی صفر (این خودِ شکست، تأیید درستیِ قفل بود) |
| `assertSceneVariety` روی جدول نهایی (از جستجوی کامل) | ✅ پاس، چهار هفته با `node` چاپ و بازرسی چشمی شد |
| Storage: فایل حذف‌شده هنوز از CDN سرو می‌شد | ✅ تأیید شد — دلیل وجود `?v={renderedAt}` |

---

## ۱۳. تست‌های هنوز لازم

۱. ~~اجرای هفتگی واقعی با کد فعلی `sceneFamily`~~ — ✅ **انجام شد** (۲۰۲۶-۰۸-۲۴، همین نشست). نتیجه در بخش «اجرای هفتگی واقعی — نتیجه» پایین همین فایل. `force` هم همان‌طور که بخش ۲ پیش‌بینی کرده بود لازم شد و کار کرد.
۲. بعد از تأیید بصری (و تصمیم درباره‌ی یافته‌ی «کپی از hint»، پایین)، **اولین هفته‌ی انتشار واقعی** روی اینستاگرام — هیچ پستی تا این لحظه منتشر نشده؛ سه چیز فقط از فید واقعی معلوم می‌شود: انسجام گرید، واکنش مخاطب، و اینکه سبک مینیمال جلب توجه می‌کند یا گم می‌شود.

---

## ۱۴. کار کامیت‌نشده یا نیمه‌تمام فعلی

⚠️ **کهنه.** هیچ کارِ کامیت‌نشده‌ای از فازِ بصری باقی نمانده — همه (شاملِ دو
اصلاحِ بعدیِ `imageSubject` و V2 STYLE) با `feat(instagram): complete visual
phase` کامیت شد.

<details><summary>محتوای تاریخی</summary>

دقیقاً بخش ۴ همین سند. خلاصه‌ی یک‌خطی: **فاز بصری کد-کامل است، تست‌شده با `npm run build` و چهار اجرای تکی، ولی نه کامیت شده نه با اجرای هفتگی واقعی تأیید شده.**

هیچ فایل نیمه‌نوشته یا خراب در ریپو نیست — هرچه در working tree است کامپایل می‌شود و منطقاً کامل است؛ فقط منتظر تأیید و کامیت است.

</details>

---

## ۱۵. بدهی فنی که نباید جلوی تکمیل پروژه را بگیرد

همه‌ی موارد بخش ۱۱ («غیربحرانی») + این‌ها از `MASTER_PLAN.md`:
- `clampText` گاهی روی تیترِ خارج‌از‌سقف یک بریدنِ آرایشیِ ناگهانیِ وسطِ جمله می‌سازد (سه‌نقطه وسطِ عبارت). یک‌بار در اعتبارسنجیِ نهاییِ هفتگیِ تنوعِ چیدمان دیده شد (۲۰۲۶-۰۸-۲۷، پایینِ همین فایل). مختصِ تنوعِ چیدمان نیست، رفتارِ عمومیِ `clampText` است، و جلوی فاز ۴ را نمی‌گیرد
- دوره‌ی چرخش `sceneFamily` سه هفته است (هفته‌ی چهارم صحنه‌ها تکرار می‌شوند) — قابل تمدید با افزودن صحنه‌ی چهارم per استخر، ولی نیازمند جستجوی مجدد برای `assertSceneVariety`
- اجازه‌ی کتبی از `siavash-smf` (صاحب ریپوی مبدأ `arkan-content-studio`) — گرفته نشده؛ ریپوی مبدأ LICENSE ندارد. این یک مسئله‌ی حقوقی/کسب‌وکاری است، نه فنی، و تصمیمش با کاربر است.

---

## ۱۶. ترتیب دقیق کارهای باقی‌مانده (پیشنهادی)

⚠️ **ردیفِ ۱ کهنه است** — انجام شد و کامیت شد. ترتیبِ به‌روز:

۱. ~~اجرای هفتگی واقعی برای تأیید `sceneFamily`~~ ✅ انجام شد → کامیت فاز بصری
۲. ~~آزمایشِ مقایسه‌ی مدل‌های تصویر~~ ✅ انجام شد → APPROVED، بدون تغییرِ کد
۳. ~~تنوع چیدمان~~ ✅ انجام شد و تأیید شد (۲۰۲۶-۰۸-۲۷) — بخش ۱۷ (زیر)
۴. **یک هفته انتشار واقعی روی اینستاگرام** — پیش‌نیاز نرم برای هر تصمیم طراحی بعدی؛ داده‌ی واقعی مخاطب چیزی است که هیچ تست داخلی نمی‌تواند جایگزینش کند
۵. **فاز ۴ — استوری** — کارِ بعدی. تا کاربر صریحاً نگفته شروعش کن، شروع نشود
۶. **فاز ۵ — رجیستری کلیدواژه‌ی دایرکت**
۷. بدهی فنی به ترتیب اولویت: `checkBrandAsSubject` انگلیسی → مسابقه‌ی درس در کمپین → backoff ۴۲۹ → بقیه

---

## ۱۷. تنوع چیدمان — ✅ تمام، پیاده و تأیید شده (۲۰۲۶-۰۸-۲۷)

⚠️ معماری در `MASTER_PLAN.md` بخش ۴ («تنوع چیدمان — ✅ تمام») آمده؛ اینجا
برای تکمیل تکرار می‌شود تا این سند به‌تنهایی کافی باشد. جزئیاتِ کاملِ
اعتبارسنجی — شش‌شکلِ بلاغی و هفته‌ی نهایی — پایینِ همین فایل.

- `Slide` (در `store/types.ts`) و `SlideSchema` (در `agents/types.ts`) →
  `discriminatedUnion` روی فیلد `layout`: `standard`/`statement`/`list`.
  `imageSubject؟` روی هر سه مشترک است.
- `slideText(s: Slide)` در `slide-spec.ts`، با `switch` و شاخه‌ی
  `default: { const never: never = s; throw }` — هم خطای کامپایل‌تایم اگر
  یک `layout` جدید بدون شاخه اضافه شود، هم `throw` زمان اجرا
- `normalizeSlide` در `socialPostFromRow`: `s?.layout ? s : {...s, layout: "standard"}`
  — داده‌ی قدیمیِ بدونِ `layout` همیشه به `standard` می‌نشیند
- `withDefaultLayout` (یک `z.preprocess` جلوِ `discriminatedUnion`): جاافتادنِ
  `layout` در خروجیِ **مدل** را به `standard` می‌بخشد و `[slide-layout]` لاگ
  می‌زند. مقدارِ `layout` که هست ولی نامعتبر است، دست‌نخورده می‌ماند تا
  اسکیما ردش کند.
- `guardPositionLayout(clampSlides(slides))` در هر دو ناشر (ترتیب مهم است):
  `list` روی کاور یا اسلایدِ آخر با `listToStandard` به `standard` تنزل
  می‌گیرد — تبدیلِ محتواگه‌دار (`items.join(" · ")`)، نه چکِ مسدودکننده.
  `statement` هیچ‌جا تنزل نمی‌گیرد.
- رندرکننده و پیش‌نمایش هر دو از `blocksFor()` (در `slide-spec.ts`) تغذیه
  می‌شوند — واگرایی‌شان ساختاراً غیرممکن است.
- `LIMITS_BY_LAYOUT` و چک «طول متن اسلایدها» چیدمان‌آگاه‌اند؛ یک چکِ
  advisoryِ جدید («تنوعِ چیدمانِ اسلایدهای میانی») یکنواختی را گزارش می‌دهد
  بدون بازنویسیِ اجباری.
- سه مصرف‌کننده به `slideText()` وصل شدند: `instagram-orchestrator.ts`،
  `repurpose-orchestrator.ts`، `feedback/route.ts`.

---

## ۱۸. تعریف نهایی «انجام‌شده» برای این پروژه

پروژه وقتی کامل است که:

۱. ✅ فاز بصری کامیت شده و با یک اجرای هفتگی واقعی تأیید شده (هفت کاور منسجم، نه یکنواخت؛ `imageSubject` داخل خانواده و بدون کپی لفظی) — **برآورده شد، ۲۰۲۶-۰۸-۲۵.**
۲. حداقل یک هفته‌ی کامل محتوا واقعاً روی اینستاگرام منتشر شده — بازخورد واقعی گرفته شده
۳. فاز ۴ (استوری) پیاده و تأیید شده — رندر ۱۰۸۰×۱۹۲۰، متن استیکر جدا از تصویر
۴. فاز ۵ (رجیستری کلیدواژه) پیاده شده — کلیدواژه‌ی دایرکت per post با پاسخ آماده
۵. تصمیم حقوقی درباره‌ی اجازه از `siavash-smf` گرفته شده (یا کاربر آگاهانه بدون آن ادامه داده)
۶. `CLAUDE.md` با آخرین تصمیم‌های معماری هم‌گام است — این فایل و `MASTER_PLAN.md` هرگز نباید از کد عقب بیفتند

**تنوع چیدمان و بدهی فنیِ غیربحرانی، پیش‌نیاز این تعریف نیستند** — می‌توانند بعد از انتشار اول انجام شوند.

---

## پاسخ به سؤال راستی‌آزمایی این سند

> اگر این گفت‌وگو ناپدید شود، آیا یک نشست تازه‌ی Claude Code می‌تواند فقط با
> `CLAUDE.md`، `MASTER_PLAN.md`، `PROJECT_STATE.md`، ریپو و تاریخچه‌ی git درست ادامه دهد؟

**بله، بدون هیچ شرطی، به‌روزشده:** نشست تازه باید اول بخشِ **«تنوع چیدمان —
بسته شد (نهایی، ۲۰۲۶-۰۸-۲۷)»** (انتهای همین فایل) را بخواند. فازِ بصری و
آزمایشِ مقایسه‌ی مدل‌های تصویر پیش از آن بسته شدند («فاز بصری — بسته شد»
چند بخش پایین‌تر). سوگیریِ مکانیکیِ اولیه (`statement` ثابت در اندیس ۱) با
دو دورِ اصلاحِ هدف‌مند و یک تشخیصِ بدون‌تغییرِکد (شش شکلِ بلاغی) دنبال شد، با
یک اجرای هفتگیِ واقعیِ نهایی تأیید شد، کاربر نتیجه را دید و فاز را تأیید
کرد، و کد کامیت شد. هیچ تصمیمِ معلقی روی هیچ‌کدام از این سه فاز باقی
نمانده؛ کارِ بعدی فاز ۴ — استوری است (تا کاربر صریحاً نگفته، شروع نمی‌شود).

---

## اجرای هفتگی واقعی — نتیجه (۲۰۲۶-۰۸-۲۴، همین نشست)

اجرا با `POST /api/social/weekly {mode:"run", weekId, force:true}` روی
`week_start=2026-08-22` (هفته‌ی از قبل موجود؛ `force` لازم بود چون این هفته
پیش‌تر، قبل از کد `sceneFamily`، یک‌بار کامل اجرا شده بود). نتیجه: **۷ از ۷
موفق**، منتقد هفته ۸۶/۱۰۰ با ۱ درس.

### ب) زمان و هزینه

- **کل درخواست: ۳۴٫۹ ثانیه** — عدد پایین‌تر از بازه‌ی مستندشده در `CLAUDE.md`
  (۴۶–۵۱ ثانیه) چون آن عدد برای **یک** اجرا بود؛ اینجا هفت اجرا کاملاً موازی
  همپوشانی داشتند (`Promise.allSettled`) — دقیقاً پیش‌بینیِ «موازی راحت جا
  می‌شود» در `CLAUDE.md` تأیید شد.
- **هزینه‌ی تصویر: ۷ فراخوانی، جمعاً $۰٫۲۳۷۲** (میانگین $۰٫۰۳۳۹ — مطابق عدد
  قبلاً اندازه‌گیری‌شده).
- هزینه‌ی متن جداگانه لاگ نمی‌شود (فقط تعداد توکن در `[llm]`)؛ برای این
  گزارش محاسبه نشد.

### الف) هفت `imageSubject` — داخل خانواده ماندند، ولی مسئله‌ی جدیدی دیده شد

هر هفت مورد از نظر **گروه بصری** (`VisualGroup`) درست بودند — هیچ نقض
`assertSceneVariety` رخ نداد و هیچ‌کدام از فضای خانواده‌ی تخصیص‌یافته‌شان
بیرون نزدند. جدول کامل (روز → استخر → `group` → hint → `imageSubject` واقعی):

| روز | استخر | گروه | hint | imageSubject واقعی |
|---|---|---|---|---|
| ۰ | digital-tech | object | کابل‌های مرتب‌شده‌ی یک رک... بدون هیچ نمایشگر | بست‌های نواری سیاه که کابل شبکه را در رک فلزی تیره مهار کرده‌اند... بدون هیچ نمایشگر |
| ۱ | academic-research | architecture | نمای بیرونی ساختمان دانشگاهی آجری در نور بعدازظهر | پله‌های سنگی ورودی دانشکده‌ی آجری قدیمی با سردر قوسی در نور مایل بعدازظهر |
| ۲ | brand (null) | interior | سالن انتظار خلوت با صندلی چرمی و نور غیرمستقیم | سالن انتظار خلوت اداری با مبل چرمی تک‌نفره و نور آباژور در گوشه |
| ۳ | entrepreneurship | nature | آسمان صبح از میان دو ساختمان، رو به بالا | آسمان روشن اول صبح، خطوط معماری دو ساختمان مدرن، کادر عمودی رو به بالا |
| ۴ | engineering-medical | threshold | راهروی خلوت مرکز درمانی با نور غیرمستقیم | روشنایی ملایم عصر روی کمدهای فلزی در راهروی خلوت مرکز تحقیقات پزشکی |
| ۵ | brand (null) | threshold | پله‌های سنگی یک ساختمان قدیمی، بدون نشانه‌ی مکان | پله‌های سنگی کهن با نور مایل صبحگاهی در ورودی ساختمان قدیمی |
| ۶ | arts-culture | interior | بوم نیمه‌کار روی سه‌پایه در کارگاه خالی، نور از پنجره‌ی سقفی | بومِ کشیده‌شده روی سه‌پایه‌ی چوبی در آتلیه‌ی نقاشی با نور پنجره‌ی قدی |

**یافته:** ۶ از ۷ مورد (همه به‌جز روز ۰) اساساً **بازنویسیِ نزدیک‌به‌عینِ خودِ
جمله‌ی hint خانواده‌اند** — نه کپی از چهار مثالِ ✅/❌ که قبلاً در
`instagram-writer.ts` اصلاح شده بودند، بلکه کپی از خودِ `hint` که `sceneBlock()`
مستقیماً در پرامپت می‌گذارد و صریحاً می‌گوید «کپیِ لفظ‌به‌لفظِ جمله‌ی بالا را
برنگردان» (`instagram-writer.ts:127`). این دستور در عمل تقریباً همیشه نادیده
گرفته شد.

این یک **مسئله‌ی جدید و جدا** از «تقلید از مثال» است که در بخش ۱۱ («بحرانی
برای ادامه‌ی کار») و در `MASTER_PLAN.md` بخش ۱۲ ثبت شده بود — آن یکی درباره‌ی
چهار مثالِ داخل `FA_BLOCK` بود، این یکی درباره‌ی خودِ `hint` در `SCENE_POOLS`.
راه‌حلی که `MASTER_PLAN.md` برای حالت تکرار پیش‌بینی کرده بود («چند صحنه‌ی
نمونه‌ی متفاوت per خانواده، نه یکی») **دقیقاً همین‌جا هم صادق است**، ولی چون
تغییر `SCENE_POOLS`/`sceneBlock()` یک تصمیم طراحی است، این نشست پیاده‌سازی‌اش
نکرد — فقط ثبت و گزارش شد.

### ج) هفت کاور کنار هم — نتیجه‌ی بازرسی چشمی

هر هفت PNG از Supabase Storage دانلود و بازرسی شدند (سیستم طراحی یکسان: کیکر
بالا-چپ، تیتر سفید بولد، بدنه، شماره‌ی صفحه پایین-راست — در همه‌ی هفت مورد
منسجم). هیچ آیکونوگرافی ممنوعه‌ی مهاجرت (پرچم/پاسپورت/چمدان) دیده نشد.

**یک جفتِ تقریباً یکسان پیدا شد:** روز ۱ (پله‌های سنگی دانشکده) و روز ۵
(پله‌های سنگی ساختمان قدیمی) — هر دو تصویری تاریک از یک **درگاهِ طاق‌دار
سنگی/آجری** با نور مایل هستند و در نگاه اول قابل تفکیک نیستند، با اینکه از
دو استخر کاملاً متفاوت (`academic-research` در برابر `brand`) آمده‌اند. این
مستقیماً محصولِ همان یافته‌ی بخش (الف) است: چون هر دو hint تقریباً بی‌تغییر
اجرا شدند و هر دو hint به‌طور مستقل «ورودی سنگیِ قدیمی» را توصیف می‌کنند،
خروجی نهایی هم یکسان از آب درآمد. بقیه‌ی پنج کاور از نظر بصری کاملاً متمایزند.

### نتیجه‌گیری و توصیه

- گروه‌بندی (`VisualGroup`/`assertSceneVariety`) کار می‌کند — قفل تنوع در سطح
  *دسته* هرگز نقض نشد.
- ولی تنوعِ *دیده‌شده در فید* به همان اندازه به کیفیت اجرای کپی‌رایتر روی هر
  hint وابسته است، و این نشست نشان داد کپی‌رایتر تقریباً همیشه hint را کلمه‌به‌کلمه
  برمی‌گرداند — که خودش می‌تواند دو خانواده‌ی متفاوت را در عمل یکسان کند (دقیقاً
  مورد روز ۱ و ۵).
- **قبل از کامیتِ نهاییِ فاز بصری، این یافته باید به کاربر گزارش شود** — تصمیم
  با اوست: (۱) همین‌طور کامیت شود و مسئله به فاز بعد موکول شود، یا (۲) یک
  دورِ کوچک روی `sceneBlock()`/`SCENE_POOLS` انجام شود (مثلاً hintها را به
  ۲–۳ گزینه در هر خانواده تبدیل کردن، یا دستور صریح‌تر «حداقل سه جزئیات تازه
  اضافه کن که در hint نیامده»).

### کار انجام‌نشده (آگاهانه، خارج از دامنه‌ی این تسک)

- کامیتِ کد فاز بصری **قبلاً** با `f0d41b1` انجام شده بود (قبل از این اجرا) —
  این نشست کد جدیدی ننوشت، فقط پایپ‌لاینِ موجود را اجرا و نتیجه را ثبت کرد.
- هیچ فیکسی برای «کپی از hint» نوشته نشد — طبق قاعده‌ی «قبل از اعمال نشانم
  بده»، این یک تصمیم طراحی است که باید اول با کاربر مطرح شود.

---

## دو اصلاح روی heritage bias (۲۰۲۶-۰۸-۲۵ — ✅ کامیت‌شده در بستنِ فاز بصری)

یافته‌ی «کپی از hint» بالا در دو نشست جدا دنبال شد — کاربر هر بار دامنه را
صریح محدود کرد، پس دو فایل جدا عوض شد، نه یکی:

### ۱) اصلاح STYLE تصویر — `src/lib/image-gen.ts`

بند «BRITISH CONTEXT» اضافه شد (نشست اول) ولی نسخه‌ی اولش («aged red
brick»، «worn wooden floorboards»، «a stone corridor») مدل را به‌جای
بریتانیای امروزی به سمت گوتیک/ویکتوریایی برد — با تصاویر واقعی تأیید شد.
اصلاح شد: صفت‌های سن‌دار حذف، فهرست محیط معاصر (کتابخانه/آزمایشگاه/فضای کار)
جایگزین، و یک بند ممنوعیت صریح («no castles, no Gothic or Victorian-heavy
architecture…») به `ABSOLUTELY EXCLUDE` اضافه شد. `DEPTH` هم نرم شد: «fully
dissolved» → «softly legible, not fully erased». **ممنوعیت آیکونوگرافی
مهاجرت دست‌نخورده ماند.** جزئیات کامل + قبل/بعد در کامنت بالای `STYLE`
(بندهای ۵ تا ۷).

نمونه‌ها: https://claude.ai/code/artifact/b33ca087-3a33-49c8-a298-12bb03483f2b
(سه مرحله: مکانی خنثی → V1 heritage-biased → V2 اصلاح‌شده)

### ۲) اصلاح منطق imageSubject — `src/lib/agents/instagram-writer.ts`

بعد از اصلاح STYLE، مقایسه نشان داد **خودِ `imageSubject`** هم مقصر بود، نه
فقط STYLE: `sceneBlock()` به کپی‌رایتر می‌گفت «imageSubject باید داخل همین
خانواده بماند» و خانواده‌ی صحنه (`SCENE_POOLS` hint) خودش معماری بود —
نتیجه: امروز و روز پنج imageSubject را عیناً معماری (پله‌ی سنگی) ساختند.

اصلاح: `sceneBlock()` حالا صریح می‌گوید خانواده فقط **پس‌زمینه** است، عنصر
کانونی باید از محتوای کاروسل بیاید. مثال‌های ✅/❌ قاعده‌ی اصلی هم از «مکان/
محیط کاری» به «شیء/سند/اقدامِ مرتبط با استدلال پست» تغییر کرد، + یک بند
جدید: مفهوم انتزاعی (پیشرفت، فرصت، مسیر شغلی) نباید خودکار به استعاره‌ی
مکانی (پله، در، راهرو) تبدیل شود.

**اعتبارسنجی واقعی:** با همان سه بریفِ واقعی (از `pipeline_runs.steps` در
Supabase، نه بازسازی‌شده) و همان `sceneFamily`، فقط گام کپی‌رایتر دوباره
اجرا شد (نه کل پایپ‌لاین):

| روز | imageSubject قدیم | imageSubject جدید |
|---|---|---|
| ۰ | بست‌های نواری سیاه... رک فلزی تیره... بدون نمایشگر | دفترچه یادداشت فنی باز با دیاگرام معماری سیستم، کنار کابل‌های رک |
| ۱ | پله‌های سنگی ورودی دانشکده‌ی آجری قدیمی... | صفحه مقاله‌ی پرینت‌شده با هایلایت زرد روی نام نویسنده‌ی سوم، در محوطه‌ی دانشگاه |
| ۵ | پله‌های سنگی کهن... ورودی ساختمان قدیمی | پوشه‌ی مدارک بسته با نوار قرمز، روی لبه‌ی پله، کنار یک خودکار فلزی |

هر سه مورد جدید حالا یک **شیء مرتبط با محتوای همان پست** دارند (دیاگرام
معماری سیستم، نام نویسنده‌ی هایلایت‌شده، پوشه‌ی مدارک)؛ معماری/پله فقط به
جزئیات پس‌زمینه تنزل کرد. نمونه‌ها: https://claude.ai/code/artifact/04169714-ec80-4ef9-bf61-f37a07992602

⚠️ **باقی‌مانده:** روز ۱ و ۵ هنوز یک اشاره‌ی کوتاه به معماری در جمله دارند
(«محوطه‌ی دانشگاه آجری»، «لبه‌ی پله»). این عمداً نگه داشته شد چون سناریوی
مطلوب است — شیء در فضای خانواده قرار می‌گیرد، نه بی‌ربط به آن. اگر کاربر
حتی همین را هم زیاد بداند، قاعده باید سخت‌گیرتر شود.

### هر دو اصلاح: تست‌شده، کامیت‌شده

`npm run build` بعد از هر دو اصلاح پاس شد. ✅ هر دو با تأییدِ نهاییِ کاربر
روی اعتبارسنجیِ هفت‌پستیِ رانده‌ی دوم (پایینِ فایل) کامیت شدند.

---

## اعتبارسنجی نهایی هفت‌پستی — راندِ اول (۲۰۲۶-۰۸-۲۵)

⚠️ **وردیکتِ این راند (NEEDS ONE TARGETED FIX) با راندِ دوم جایگزین شد** —
بعد از اعمالِ اصلاحِ پیشنهادی‌اش، اعتبارسنجیِ راندِ دوم (پایینِ فایل) وردیکتِ
✅ READY TO CLOSE داد. این بخش به‌عنوانِ ریشه‌ی مشکل و شواهدِ اولیه نگه داشته
شده، نه به‌عنوانِ وضعیتِ فعلی.

بعد از تأیید کاربر روی مقایسه‌ی سه‌تایی بالا، یک اجرای **واقعی و کامل
هفت‌پستی** با کد فعلی گرفته شد (`force:true` روی هفته‌ی موجود
`2026-08-22`) — STYLE نسخه‌ی دوم + منطق اصلاح‌شده‌ی imageSubject +
sceneFamily بدون هیچ تغییر جدید. هدف فقط اعتبارسنجی بود، نه تغییر کد.

**نتیجه:** ۷ از ۷ اسلات موفق، ۳۲٫۳ ثانیه (اجرای موازی)، هزینه‌ی تصویر
$۰٫۲۳۹۴ (۷ فراخوانی)، بدون هیچ تلاش مجدد یا شکست. منتقد هفته: ۸۴/۱۰۰، ۲ درس.

**جدول کامل + داوری تک‌تک پست‌ها + ممیزی تکرار در گرید:**
https://claude.ai/code/artifact/349ddddb-a95b-478a-ae30-329113d70330

**آیا مشکل قبلیِ imageSubject حل شد؟ تا حدی.** پنج از هفت پست (۰، ۲، ۳، ۴،
۶) شیء‌محورند و خانواده‌ی صحنه فقط اتمسفر پس‌زمینه ماند — اصلاح در این پنج
مورد کار کرد. اما:

- **روز ۱** (پارافریزِ نزدیک): imageSubject محیط را با عبارتی نزدیک به
  خودِ hint توصیف کرد («حیاط یک دانشکده آجری» ≈ hint «ساختمان دانشگاهی
  آجری») — نسخه‌ی ملایمِ همان مشکلِ کپی.
- **روز ۵** (بازگشتِ کامل الگو): imageSubject عبارت «پله‌های سنگی» را
  تقریباً کلمه‌به‌کلمه از hint خانواده‌ی `brand/threshold` گرفت، و در
  تصویر نهایی هم پله به‌وضوح غالب قاب است — دقیقاً همان الگوی ممنوعِ
  «پیشرفت = پله» که قرار بود حذف شود.

**ریشه (تحلیل، نه اثبات‌شده با آزمایش بیشتر):** استخر `brand` در
`SCENE_POOLS` (`weekly-grid.ts`) فقط ۴ گزینه دارد و یکی از آن‌ها («پله‌های
سنگی یک ساختمان قدیمی») خودش یک ویژگی معماریِ عینی و نام‌دار است، نه یک
توصیف نور/حس. `sceneBlock()` می‌گوید خانواده فقط پس‌زمینه است، ولی وقتی
خودِ hint یک اسم معماریِ مشخص باشد، مدل تمایل دارد آن اسم را عیناً به
imageSubject منتقل کند. این ریسک تصادفی نیست: هر بار مسیر `brand` این
صحنه را بگیرد (۱ در ۴)، احتمال بازگشت این الگو هست.

**وضعیت فاز بصری در این راند: NEEDS ONE TARGETED FIX** (نه READY TO CLOSE) —
✅ این اصلاح در ادامه پیاده و اعتبارسنجی شد؛ نتیجه پایینِ فایل.

پیشنهادِ کوچک‌ترین اصلاح (در آن لحظه فقط پیشنهاد بود؛ ✅ اکنون پیاده‌شده —
نگاه کن به «اصلاح دومِ imageSubject» زیر): در قاعده‌ی imageSubject یک بند
اضافه شود — وقتی خودِ scene hint یک ویژگی معماریِ نام‌دار دارد (پله، در،
راهرو، نما)، نورش را نگه دار ولی اسمِ خودِ ویژگی را در imageSubject تکرار
نکن. این فقط به قاعده‌ی موجود در `instagram-writer.ts` یک شرط اضافه
می‌کند؛ نه `SCENE_POOLS` و نه STYLE را دست می‌زند.

`npm run build` بعد از این اجرا (بدون تغییر کد) پاس شد. هیچ فایلی در این
اجرا تغییر نکرد؛ فقط این سند به‌روزرسانی شد.

---

## اصلاح دومِ imageSubject (۲۰۲۶-۰۸-۲۵ — ✅ کامیت‌شده در بستنِ فاز بصری)

روی همان دو مشکلِ باقی‌مانده از اعتبارسنجیِ هفت‌پستیِ بالا، یک اصلاحِ کوچکِ
دیگر فقط در `src/lib/agents/instagram-writer.ts` اعمال شد — بدون دست‌زدن به
STYLE (`image-gen.ts`) یا `SCENE_POOLS` (`weekly-grid.ts`).

**دو مشکل:**

۱. **نشتِ اسمِ معماری** — وقتی خودِ scene hint یک اسمِ معماریِ مشخص داشت
   (پله، در، راهرو، نما، طاق، مسیر، پنجره)، مدل همان اسم را عیناً به
   imageSubject می‌آورد، حتی وقتی شیءِ اصلی درست بود.
۲. **همگراییِ نوعِ موضوع** — هر هفت imageSubject اجرای هفتگیِ قبلی به یک
   آرکی‌تایپ همگرا شدند: سند/برگه/پوشه روی یک سطح. ریشه‌اش خودِ مثال‌های ✅
   قاعده بود — هر چهارتا از جنسِ «سند» بودند (همان درسِ قدیمیِ «مثال
   هم‌جنس، خروجی هم‌جنس می‌سازد»، تکرارشده در سطحِ نوعِ موضوع).

**اصلاح:**

- در `sceneBlock()`: یک هشدارِ صریح اضافه شد — اگر جمله‌ی خانواده‌ی صحنه
  یکی از آن اسم‌های معماری را دارد، آن اسم نباید عیناً در imageSubject
  تکرار شود؛ فقط نور/حسِ فضا می‌ماند.
- در قاعده‌ی اصلیِ imageSubject: همان هشدار به‌صورتِ عمومی (نه فقط
  وابسته به sceneFamily) اضافه شد + یک بندِ «نوعِ موضوع» که هفت نوعِ مجازِ
  موضوعِ کانونی را فهرست می‌کند (سند، شیء فنی/ابزار، اقدام/ژست، نتیجه‌ی
  فیزیکیِ کار، جزئیاتِ محیط حرفه‌ای، تضادِ دو شیء) + یک **خودآزمونِ سند**
  صریح: «اگر سند/برگه/پوشه انتخاب می‌کنی، آیا واقعاً قوی‌ترین بازنماییِ
  همین پست است یا فقط راحت‌ترین گزینه؟». مثال‌های ✅ هم از چهار سند به
  ترکیبی از سند، اقدام/ژست، شیء فنی، و تضادِ دو شیء تغییر کردند. یک مثالِ
  ❌ جدید هم اضافه شد: «پوشه‌ای روی پله‌های سنگی» — یعنی حتی وقتی شیء درست
  است، جاگذاریِ آن روی اسمِ معماریِ خانواده همان مشکل را با یک لایه‌ی اضافه
  تکرار می‌کند.

**اعتبارسنجی واقعی:** با ۴ بریفِ واقعی از همان اجرای هفتگی (نه بازسازی‌شده):
روز ۱ و روز ۵ (دو پستِ مشکل‌دار)، + روز ۰ و روز ۴ (دو پستِ سندمحورِ قبلی که
نوعِ غیرسند برایشان منطقی بود — دیجیتال‌تک و مهندسی/R&D). کاور واقعی برای
هر چهار تولید شد (STYLE بدون تغییر، بدون کامپوزیتِ متن روی تصویر تا فقط
اثرِ خودِ imageSubject جدا دیده شود).

| روز | imageSubject قدیم | imageSubject جدید | نوعِ جدید |
|---|---|---|---|
| ۰ | دیاگرام معماری سیستم کنار کابل‌های شبکه | سرور بلید بیرون‌کشیده از رک با برچسبِ مشخصاتِ معماری سیستم | شیء فنی |
| ۱ | مقاله + هایلایتِ نام نویسنده، **در حیاطِ دانشکده‌ی آجری** | مقاله + هایلایتِ نامِ نویسنده‌ی دوم، کنار یادداشتِ دست‌نویس روی میزِ چوبی | سند (بدون هیچ اشاره‌ی معماری) |
| ۴ | پوشه‌ی فنی با برچسبِ R&D **در راهرو** | کولیسِ فلزی + مستنداتِ فنی با جدولِ بنچمارک روی میزِ کار | شیء فنی + سند |
| ۵ | پوشه‌ی مدارک **روی پله‌های سنگی** | دفترچه‌ی یادداشتِ باز با دو ستونِ مقایسه، روی یک **سطحِ سنگی** (بدون پله) | سند (دفترچه‌ی شخصی) |

نتیجه‌ی چهارتایی: https://claude.ai/code/artifact/3444b971-b51f-4c1f-ad09-9e851e912493

**نتیجه‌گیریِ صادقانه:**

- **نشتِ اسمِ معماری در هر ۴ مورد رفع شد** — هیچ‌کدام از هفت اسمِ ممنوع
  (پله/در/راهرو/نما/طاق/مسیر/پنجره) در چهار imageSubject جدید نیامد.
  ⚠️ روز ۵ هنوز کلمه‌ی «سنگی» (جنسِ ماده، نه اسمِ معماری) را نگه داشته —
  این عمداً بیرون از فهرستِ ممنوع است چون فقط جنس را از حسِ خانواده وام
  می‌گیرد، نه خودِ عنصرِ معماری را؛ در تصویرِ نهایی هم دیگر هیچ پلکانی دیده
  نمی‌شود.
- **همگراییِ نوعِ موضوع تا حدی رفع شد**، نه کامل: روز ۰ و روز ۴ به شیء فنی
  تغییر کردند (دقیقاً هدفِ اصلاح)، ولی روز ۱ و روز ۵ همچنان سند ماندند.
  این شکست نیست — هر دو پست واقعاً درباره‌ی یک مدرکِ مکتوب‌اند (فهرستِ
  نویسندگان، مقایسه‌ی نوشتاری) و قاعده‌ی جدید صریح می‌گوید سند را زورکی
  عوض نکن اگر محتوا واقعاً سند می‌طلبد. نمی‌توان از همین ۴ نمونه نتیجه
  گرفت که مشکلِ همگرایی در سطحِ کل هفته کاملاً حل شده — فقط اعتبارسنجی شد
  که مدل **می‌تواند** وقتی مناسب است نوع را عوض کند.

`npm run build` پاس شد. فایلِ تغییرکرده فقط `src/lib/agents/instagram-writer.ts`
(+ همین سند). `image-gen.ts` و `weekly-grid.ts` در این اصلاح دست نخوردند.
✅ در بستنِ فاز بصری کامیت شد. طبق دستورِ کاربر در آن لحظه، بعد از همین
چهار پست متوقف شد؛ اجرای هفتگیِ کاملِ بعدی در راندِ دوم (زیر) گرفته شد.

---

## اعتبارسنجیِ نهاییِ هفت‌پستی — راندِ دوم (۲۰۲۶-۰۸-۲۵ — ✅ نهایی، کامیت‌شده)

اجرای واقعیِ هفتِ پستِ کامل با کدِ فعلی — STYLE بریتانیایی V2 + اصلاحِ اول
imageSubject + اصلاحِ دوم (نشتِ معماری + خودآزمونِ سند) + sceneFamily
قفل‌شده — بدون هیچ تغییرِ پرامپت پیش از اجرا. همان هفته‌ی `2026-08-22`
(`weekId 624247d8-c893-4bc3-9c61-ff12853e71fc`) با `force: true` دوباره
اجرا شد.

**نتیجه: ۷ از ۷ موفق، ۳۱٫۲s، منتقد ۸۶/۱۰۰ (۱ درس).**

⚠️ **حادثه‌ی عملیاتی در حینِ اجرا:** ۵ از ۷ فراخوانیِ `generateCoverImage`
با `402 Insufficient credits` از OpenRouter شکست خوردند (سقفِ اعتبارِ
حساب رسیده بود: `total_usage 4.32` از `total_credits 5`) — نه خطای کد.
مسیرِ محافظِ طراحی‌شده در `storage.ts` (شکستِ تولیدِ تصویر بی‌ضرر است، کاور
پس‌زمینه‌ی پیش‌فرض می‌گیرد) درست کار کرد و پایپ‌لاین را نشکست، ولی یعنی
۵ پست موقتاً کاورِ واقعی نداشتند. بعد از افزایشِ اعتبار توسط کاربر
(`total_credits` → ۱۵)، همان ۵ پست با دکمه‌ی «رندر دوباره» (بدون هیچ
تغییری در imageSubject یا محتوا — همان مکانیزمی که برای همین سناریو
طراحی شده) بازتولید شدند و هر ۷ کاور اکنون از تولیدِ واقعیِ AI هستند
(تأییدشده با دانلودِ مستقیمِ `bg-cover.png` هر ۷ پست از Storage).
هزینه‌ی کلِ ۷ تصویر: **$۰٫۲۳۸۱**.

### جدولِ کامل

| روز | مسیر | زبان | audienceGroup | خانواده‌ی صحنه | هینت | imageSubject | نوعِ موضوع |
|---|---|---|---|---|---|---|---|
| شنبه (۰) | global-talent | fa | digital-tech | object | کابل‌های رک، بدون نمایشگر | پچ‌پنل سرور با کابل‌های شبکه و برچسبِ دست‌نویسِ نامِ سرورها | شیء فنی |
| یکشنبه (۱) | global-talent | fa | academic-research | architecture | نمای دانشگاهیِ آجری، بعدازظهر | پوشه‌ی پژوهشیِ ضخیم با برچسبِ گرنت، روی میزِ چوبی، نورِ گرمِ بعدازظهر | سند |
| دوشنبه (۲) | brand | en | — | interior | سالنِ انتظار، صندلیِ چرمی | پوشه‌ی چرمیِ نیمه‌باز با برگه‌ی ارزیابیِ علامت‌خورده | سند |
| سه‌شنبه (۳) | innovator-founder | fa | entrepreneurship | nature | آسمانِ صبح میانِ دو ساختمان | طرحِ کسب‌وکارِ چاپی کنارِ پنجره، نمودارِ درآمدیِ علامت‌گذاری‌شده | سند |
| چهارشنبه (۴) | global-talent | fa | engineering-medical | threshold | راهرویِ خلوتِ مرکزِ درمانی | قطعه‌ی صنعتیِ برش‌خورده + کولیسِ فلزی روی میزِ کار | شیء فنی |
| پنجشنبه (۵) | brand | en | — | threshold | **پله‌های سنگی**، بدون نشانه‌ی مکان | proposal + diagram روی **سنگِ فرسوده** (بدون پله) | تضادِ دو سند |
| جمعه (۶) | global-talent | fa | arts-culture | interior | بومِ نیمه‌کار روی سه‌پایه | کاتالوگ‌های نمایشگاه + نشانِ فستیوال + جعبه‌رنگ روی میزِ کارگاه | نتیجه‌ی فیزیکیِ کار |

### ممیزیِ نشتِ معماری

هر ۷ اسمِ ممنوع (پله/در/راهرو/نما/طاق/مسیر/پنجره) بررسی شد. **حل‌شده در
۶ از ۷ مورد**، از جمله دقیقاً همان هینتِ «پله‌های سنگی» که در اولین
اعتبارسنجی (پیش از اصلاحِ دوم) به «پوشه‌ای روی پله‌های سنگی» منجر شده
بود — این بار به «سنگِ فرسوده» (جنس، نه عنصرِ معماری) تبدیل شد و در
تصویرِ نهایی هیچ پلکانی دیده نمی‌شود.

⚠️ **یک نشتِ خفیف باقی‌مانده:** سه‌شنبه — «پنجره» (یکی از هفت اسمِ ممنوع)
در imageSubject آمده، ولی نه از جمله‌ی هینت (که اصلاً پنجره‌ای در آن
نبود) بلکه از خودِ مدل، و فقط به‌عنوانِ توصیفِ محل («میزِ کنارِ پنجره»)
نه موضوعِ کانونی. در تصویرِ رندرشده هم فقط یک لکه‌ی محوِ پس‌زمینه است، نه
عنصرِ غالب. جدی نیست، ولی نشان می‌دهد قاعده «از خودت هم وام نگیر» صددرصد
بسته نشده.

### ممیزیِ همگراییِ نوعِ موضوع

توزیعِ نهایی: ۲ شیء فنی (۰، ۴)، ۴ سند (۱، ۲، ۳، ۵ — که ۵ خودش تضادِ دو
سند است)، ۱ نتیجه‌ی فیزیکیِ کار (۶). سند هنوز اکثریت دارد، ولی برخلافِ
اعتبارسنجیِ اولِ راندِ اول (که ۷ از ۷ سند بود)، این‌بار **بیشترِ
موارد از محتوای واقعیِ پست می‌آیند**: یکشنبه درباره‌ی خودِ CV است، سه‌شنبه
درباره‌ی طرحِ کسب‌وکارِ رد‌شده، پنجشنبه درباره‌ی مقایسه‌ی دو نوع شاهد — سند
در این سه مورد بازنماییِ درست است، نه انتخابِ تنبلانه. تنها موردِ مرزی
دوشنبه است، جایی که یک بازنماییِ غیرسندی (مثلاً حرکتِ دست روی متن) هم
منطقی بود.

### ممیزیِ تکرارِ بصری (روی هفت کاورِ واقعی)

- معماریِ غالب بر قاب: **تمیز** — هیچ پله/در/راهرو/طاق به‌عنوانِ موضوعِ
  کانونی در هیچ‌کدام از ۷ تصویر.
- سوگیریِ میراثی/گوتیک/ویکتوریایی: **تمیز** — پس‌زمینه‌ها مدرن و خنثی‌اند.
- آیکونوگرافیِ مهاجرت/سفر: **تمیز** — هیچ چمدان/هواپیما/پاسپورت.
- لحنِ کسل/تاریک: پالتِ سرمه‌ای-سبزِ برند در هر ۷ تصویر تکرار می‌شود، ولی
  این هماهنگیِ برندِ عمدی است، نه یکنواختیِ خسته‌کننده — نورپردازی در هر
  کاور متفاوت است (بعدازظهرِ گرم، صبحِ ملایم، نورِ سرد).
- ترکیب‌بندی/زاویه‌ی دوربین: مشابه در هر ۷ (نزدیک/میانی روی یک سطح) —
  خودِ الگویِ STYLE است، نه نقص؛ تنوعِ خودِ شیء کافی برای تمایز است.
- انسجامِ گرید: **تمیز** — هفت کاور هویتِ بصریِ واحد می‌سازند و در همان
  حال هرکدام با نگاهِ اول قابل‌تشخیص از بقیه است.

نتیجه‌ی هفت‌کاوره: https://claude.ai/code/artifact/90b3364d-4b8a-4beb-91aa-4c398c70907f

### وردیکتِ نهایی: ✅ READY TO CLOSE

هر ۵ معیارِ READY TO CLOSE برآورده شد: ۷/۷ موفق؛ شکستِ قدیمیِ
پله/معماری حل شده (شاملِ همان هینتِ دقیقِ مشکل‌ساز)؛ imageSubjectها
معنادار و متصل به محتوای پست‌اند؛ همگراییِ سندی جدی باقی نمانده (اکثریتِ
موارد توجیه‌شده‌اند، فقط یک موردِ مرزی)؛ گرید منسجم ولی به‌اندازه‌ی کافی
متنوع است. دو نکته‌ی جزئیِ باقی‌مانده (نشتِ «پنجره» در سه‌شنبه، مرزی‌بودنِ
دوشنبه) به‌اندازه‌ای نیستند که خروجیِ عادیِ هفتگی را واقعاً خراب کنند —
پس تحتِ معیارِ «NEEDS ONE TARGETED FIX» (که فقط برای نقصِ روشن و
تکرارشونده با آسیبِ واقعی تعریف شده) قرار نمی‌گیرند؛ صرفاً برای پیگیریِ
آینده ثبت می‌شوند، نه اصلاحِ فوری.

`npm run build` پاس شد. هیچ فایلِ کدی در این راند تغییر نکرد — فقط
اجرای پایپ‌لاین (API واقعی) و همین سند.

کاربر این نتیجه را دید و **فاز بصری را تأیید کرد**. کدِ انباشته‌شده
(`image-gen.ts` + `instagram-writer.ts`) به‌همراه این سند با
`feat(instagram): complete visual phase` کامیت شد.

---

## فاز بصری — بسته شد (نهایی، ۲۰۲۶-۰۸-۲۵)

**وضعیت: ✅ COMPLETE.** این بخش مرجعِ نهایی است — هر نشستِ تازه‌ای که این
فایل را می‌خواند، فقط همین بخش را لازم دارد؛ بخش‌های بالاتر روایتِ فرایند
است، نه وضعیتِ فعلی.

### نتیجه‌ی اعتبارسنجیِ نهایی

- اجرای واقعیِ هفت‌پستیِ کامل: **۷ از ۷ موفق، ۳۱٫۲ ثانیه**، منتقد ۸۶/۱۰۰
- هزینه‌ی هفت تصویرِ کاور: **$۰٫۲۳۸۱**
- آرتیفکتِ مقایسه‌ی هفت کاور: https://claude.ai/code/artifact/90b3364d-4b8a-4beb-91aa-4c398c70907f
- وردیکت: **✅ READY TO CLOSE** — هر پنج معیار برآورده شد (جزئیات در بخشِ بالا)

### حادثه‌ی عملیاتیِ حینِ اجرا (برای آگاهیِ نشست‌های بعدی)

در حینِ اجرای نهایی، حسابِ OpenRouter به سقفِ اعتبار رسید و ۵ از ۷
فراخوانیِ `generateCoverImage` با `402 Insufficient credits` شکست خوردند —
نه خطای کد. مسیرِ محافظِ موجود در `storage.ts` (شکستِ تولیدِ تصویر بی‌ضرر
است، کاور پس‌زمینه‌ی پیش‌فرض می‌گیرد) درست کار کرد. کاربر اعتبارِ حساب را
افزایش داد و همان پنج پست با «رندر دوباره» (بدون هیچ تغییری در
`imageSubject` یا محتوا) با موفقیت بازتولید شدند. هر ۷ کاورِ نهایی از
تولیدِ واقعیِ AI هستند — تأییدشده با دانلودِ مستقیمِ فایل از Storage، نه
فقط از لاگِ اجرا.

### دو نکته‌ی جزئیِ غیرمسدودکننده (برای پیگیریِ آینده، نه اصلاحِ فوری)

۱. **یک اشاره‌ی محیطیِ «پنجره»** در imageSubjectِ روزِ سه‌شنبه — خودِ مدل
   اضافه‌اش کرد (نه از جمله‌ی هینت)، فقط به‌عنوانِ توصیفِ محل، و در تصویرِ
   نهایی فقط یک لکه‌ی محوِ پس‌زمینه است.
۲. **یک انتخابِ سندیِ مرزی** در روزِ دوشنبه — یک بازنماییِ غیرسندی هم
   منطقی بود، ولی انتخابِ سند هم قابل‌دفاع است.

### هیچ مانعِ باقی‌مانده‌ای برای فازِ بصری وجود ندارد

STYLE، `sceneFamily`، و منطقِ `imageSubject` قفل‌اند و نباید بدونِ دلیلِ
تازه دوباره باز شوند. کارِ بعدی **تغییرِ این فاز نیست** — آزمایشِ مدل‌های
تصویر است، در زیر.

### کارِ بعدی: آزمایشِ مقایسه‌ی مدل‌های تصویر (Visual Model Benchmark)

⚠️ این یک **آزمایشِ مجزا** است، نه ادامه‌ی فازِ بصری و نه ادغامِ ارائه‌دهنده‌ی
جدید. تا کاربر صریحاً نگفته شروعش کن، شروع نشود.

همان `imageSubject`های ثابت‌شده و همان STYLE نهایی را روی این چهار مدل
امتحان کن:

۱. مدلِ فعلی — `google/gemini-3.1-flash-lite-image` (Nano Banana 2 Lite)
۲. Nano Banana 2
۳. Nano Banana Pro
۴. Higgsfield — اگر Soul 2.0 در دسترس بود، همان

معیارهای مقایسه: پایبندی به `imageSubject`، کیفیتِ بصری، کیفیتِ زمینه‌ی
بریتانیاییِ معاصر، انسجام با برند، تنوعِ بصری، گرایش به «عکسِ استوک»/غیرواقعی
بودن، تأخیر (latency)، و هزینه.

هیچ ارائه‌دهنده‌ی جدیدی در این مرحله به کد وصل نمی‌شود — فقط مقایسه.
Canva بخشی از این آزمایش نیست؛ اگر لازم شد، جداگانه برای گردشِ کار
قالب/رندر ارزیابی می‌شود، نه اینجا.

بعد از آزمایش، **کاربر** مدل/ارائه‌دهنده‌ی نهایی را انتخاب می‌کند — قبل از
ادامه به فازِ «تنوع چیدمان»، فازِ استوری، یا رجیستری کلیدواژه.

⚠️ **این بخش هم اکنون کهنه است.** آزمایش انجام شد (APPROVED، بدون تغییرِ
کد) و تنوعِ چیدمان هم بعدش تمام شد. برای وضعیتِ واقعی به بخشِ «تنوع چیدمان —
بسته شد» در انتهای فایل مراجعه کن.

---

## آزمایشِ مقایسه‌ی مدل‌های تصویر — نتیجه (۲۰۲۶-۰۸-۲۵)

**وضعیتِ تصمیم: ✅ APPROVED (۲۰۲۶-۰۸-۲۵).** این یک آزمایشِ مجزا بود — هیچ
تغییری در `image-gen.ts`، `instagram-writer.ts`، یا `weekly-grid.ts` اعمال
نشد. همه‌ی اسکریپت‌ها و تصاویر در scratchpad نشست ماندند، خارج از ریپو.

### مدل‌های واقعاً تست‌شده

| # | مدل | ارائه‌دهنده | نتیجه |
|---|---|---|---|
| ۱ | `google/gemini-3.1-flash-lite-image` (پایه‌ی فعلی) | OpenRouter | ✅ ۳/۳ |
| ۲ | `google/gemini-3.1-flash-image` (Nano Banana 2) | OpenRouter | ✅ ۳/۳ |
| ۳ | `google/gemini-3-pro-image` (Nano Banana Pro) | OpenRouter | ✅ ۳/۳ |
| ۴ | `text2image_soul_v2` (Higgsfield Soul 2.0) | Higgsfield CLI | ✅ ۳/۳ (تولید موفق، ولی کیفیتِ نامناسب — پایین را ببین) |

هر ۱۲ فراخوانی (۳ موضوع × ۴ مدل) بدون شکست یا تلاشِ مجدد موفق شدند.

### سه imageSubject تست‌شده (عیناً از جدولِ نهاییِ هفت‌پستی، بدون تغییر)

۱. **شیء فنی/ابزار** (روز شنبه، global-talent/fa): «پچ‌پنل سرور با کابل‌های
   شبکه و برچسبِ دست‌نویسِ نامِ سرورها»
۲. **سند/مدرک** (روز دوشنبه، brand/en): «پوشه‌ی چرمیِ نیمه‌باز با برگه‌ی
   ارزیابیِ علامت‌خورده»
۳. **نتیجه‌ی فیزیکیِ کار** (روز جمعه، global-talent/fa): «کاتالوگ‌های
   نمایشگاه + نشانِ فستیوال + جعبه‌رنگ روی میزِ کارگاه»

### آرتیفکتِ مقایسه‌ی ۱۲ تصویر

https://claude.ai/code/artifact/9db744af-5bd1-4c7a-bf93-900815309cc9

جدولِ کامل به‌همراهِ نمره‌دهیِ ۱ تا ۱۰ روی معیارهای پایبندی/کیفیت/زمینه‌ی
بریتانیایی/برند/تنوع/واقع‌گرایی، جدولِ latency/cost، و توضیحِ محدودیتِ نسبتِ
تصویر — همه در آرتیفکت.

### هزینه و تأخیر (خلاصه)

| مدل | میانگینِ latency | میانگینِ cost/تصویر | مجموعِ cost |
|---|---|---|---|
| Nano Banana 2 Lite | ۳٫۳s | $۰٫۰۳۴۳ | $۰٫۱۰۲۸ |
| Nano Banana 2 | ۱۱٫۵s | $۰٫۰۶۸۵ | $۰٫۲۰۵۴ |
| Nano Banana Pro | ۲۰٫۹s | $۰٫۱۳۷۳ | $۰٫۴۱۱۹ |
| Higgsfield Soul 2.0 | ۲۳–۶۲s (پراکنده) | ۰٫۱۲ کردیت (رایگان، نه $) | ۰٫۳۶ کردیت |

### محدودیتِ نسبتِ تصویر

Higgsfield Soul 2.0 نسبتِ ۴:۵ بومی ندارد (فهرستِ پشتیبانی‌شده:
`1:1, 16:9, 9:16, 4:3, 3:2, 2:3, 3:4`). نزدیک‌ترینِ عمودی `3:4` انتخاب شد و
بدونِ برش گزارش شد. مدل‌های گوگل هر سه نزدیکِ ۴:۵ بومی دادند (۹۲۸×۱۱۵۲،
نسبتِ ۰٫۸۰۶ — نه دقیقاً ۱۰۸۰×۱۳۵۰ ولی خیلی نزدیک‌تر از ادعای قدیمیِ
`image-gen.ts`).

### یافته‌ی اصلی: Higgsfield با این STYLE دقیق شکست خورد

هر سه تصویرِ Higgsfield چند بندِ `ABSOLUTELY EXCLUDE` را نقض کردند: متنِ
شبه‌انگلیسیِ مخدوش و بزرگ در هر سه (به‌نظر می‌رسد پاراگراف‌های ساختاریافته‌ی
STYLE به‌جای دستورالعمل به‌عنوانِ متنِ قابلِ‌رندر خوانده شده‌اند)، کولاژِ
دوقاب/چندقاب در دو مورد، یک چهره‌ی کاملاً قابل‌تشخیص در یک مورد (دقیقاً
ریسکی که قاعده‌ی ۴ در `CLAUDE.md` برایش نوشته شده)، یک لوگوی کوچک در یک
مورد، و بازگشتِ معماریِ پلکان/آجریِ ممنوع در دو مورد. نُه تصویرِ گوگل با
همان STYLE دقیق هیچ‌کدام از این نقض‌ها را نداشتند — پس علتِ محتمل، ناسازگاریِ
Soul 2.0 با فرمتِ پرامپتِ طولانی/ساختاریافته‌ی ماست، نه لزوماً توانِ کلیِ
مدل. این فرضیه با یک پرامپتِ کوتاه‌تر قابلِ‌آزمایش است ولی خارج از دامنه‌ی
همین بنچمارک بود.

### طبقه‌بندی چهار کاندید

| مدل | طبقه‌بندی |
|---|---|
| Nano Banana 2 Lite | **PRODUCTION DEFAULT** |
| Nano Banana 2 | SPECIAL-USE OPTION |
| Nano Banana Pro | PREMIUM OPTION |
| Higgsfield Soul 2.0 | **NOT WORTH INTEGRATING** |

### تصمیمِ نهاییِ کاربر (۲۰۲۶-۰۸-۲۵ — ✅ APPROVED)

**مدلِ تولید: `google/gemini-3.1-flash-lite-image` (Nano Banana 2 Lite) —
بدون تغییر.**

**دلیلِ تصمیم:** بهترین موازنه‌ی کیفیت/تأخیر/هزینه/سازگاری با پایپ‌لاینِ
فعلی. Nano Banana 2 و Nano Banana Pro بهبودِ کیفیت نشان دادند (به‌خصوص Pro
در واقع‌گرایی/بافت)، ولی نه به‌اندازه‌ای که هزینه و تأخیرِ اضافه‌شان را برای
تولیدِ هفتگیِ معمول توجیه کند (Nano Banana 2 ≈۲× هزینه/≈۳٫۵× تأخیر؛ Pro ≈۴×
هزینه/≈۶٫۳× تأخیر — نگاه کن به جدولِ بالا).

**تصمیم‌های صریح:**

۱. `google/gemini-3.1-flash-lite-image` پیش‌فرضِ تولید می‌ماند.
۲. Nano Banana 2 در این مرحله به تولید وصل **نمی‌شود**.
۳. Nano Banana Pro در این مرحله به تولید وصل **نمی‌شود**.
۴. Higgsfield/Soul به پایپ‌لاینِ تولید وصل **نمی‌شود** — بر پایه‌ی همین
   بنچمارک ادغام تأیید نشد (نقضِ قواعدِ سختِ برند: چهره‌ی قابل‌تشخیص، متنِ
   مخدوش، کولاژ — نه صرفاً نسبتِ تصویر).
۵. **هیچ لایه‌ی انتزاعِ ارائه‌دهنده (provider abstraction) و هیچ ردیفِ
   پریمیوم ساخته نمی‌شود.** بنچمارک هزینه‌ی این پیچیدگیِ معماری را توجیه
   نمی‌کند.
۶. **هیچ تغییرِ کدِ تولید لازم نیست** — `image-gen.ts` همان‌طور که هست
   می‌ماند.

**آزمایشِ آینده‌ی Higgsfield/Moodboard — 🔵 NON-BLOCKING / FUTURE
EXPERIMENT.** اگر روزی دنبال شد، باید با پرامپتِ کوتاه‌ترِ بومیِ مدل +
تصاویرِ مرجع/مودبورد فومنت باشد، و برای محتوای hero/کمپین، نه تولیدِ
هفتگیِ معمول. این مسیر بخشی از نقشه‌راهِ فعلیِ اینستاگرام **نیست** و نباید
جلوی ادامه‌ی پروژه را بگیرد.

Canva جدا می‌ماند — ارزیابیِ آینده برای قالب/رندر/قابلیتِ‌ویرایش، بی‌ربط به
تصمیمِ مدلِ تصویر.

### فایل‌های تغییرکرده در ریپو

**هیچ‌کدام از کدِ تولید.** `image-gen.ts`، `instagram-writer.ts`،
`weekly-grid.ts` دست نخوردند. اسکریپتِ بنچمارک و ۱۲ تصویرِ خام در scratchpad
نشست ماندند (خارجِ ریپو). فقط این سند و `MASTER_PLAN.md` تغییر کردند.

### کارِ بعدی (کهنه — ببین پایینِ فایل)

⚠️ تنوعِ چیدمان از این نقطه انجام شد و تمام شد. بخشِ «تنوع چیدمان — بسته
شد» در انتهای همین فایل مرجعِ واقعی است.

---

## تنوع چیدمان — بسته شد (نهایی، ۲۰۲۶-۰۸-۲۷)

**وضعیت: ✅ COMPLETE.** این بخش مرجعِ نهایی است — هر نشستِ تازه‌ای که این
فایل را می‌خواند، فقط همین بخش را لازم دارد؛ بخش‌های بالاتر (از جمله «طرح
تنوع چیدمان» در بخش ۱۷) روایتِ فرایند و معماری‌اند، نه وضعیتِ فعلی.

### توالیِ اعتبارسنجی

۱. **آفلاینِ Stage 1** — تست منطقِ خالص روی `blocksFor()`/`slideText()` برای هر
   سه چیدمان، شکستِ عمدیِ شاخه‌ی `never`، بدترین‌حالتِ `listToStandard`،
   `guardPositionLayout` — همه پاس.
۲. **زنده‌ی فارسی/انگلیسی Stage 2** — اجراهای واقعیِ `runInstagramWriter` روی
   چند بریف، هر سه چیدمان با محتوای واقعی ظاهر شدند؛ رگرسیونِ بایت‌به‌بایتِ
   رندر روی یک پستِ قدیمیِ بدونِ `layout` تأیید شد (بدونِ تغییر).
۳. **اولین اعتبارسنجیِ هفتگی** یک سوگیریِ مکانیکی را کشف کرد: `statement`
   دقیقاً در اسلایدِ اندیس ۱ در ۷ از ۷ کاروسل — ریشه‌اش ترتیبِ مثالِ
   `SHAPE_HINT`/`SHAPE_HINT_EN` در `instagram-writer.ts` بود.
۴. **اصلاحِ هدف‌مندِ نویسنده/SHAPE_HINT** — بازآراییِ مثال + قواعدِ معناییِ
   جدید (بدونِ موقعیتِ ثابت برای `statement`/`list`، عدمِ اجبارِ «دقیقاً یکی
   از هرکدام»). اعتبارسنجیِ ۴موردی: موقعیتِ `statement` دیگر ثابت نبود.
   یک الگوی باقی‌مانده دیده شد: هر ۴ مورد دقیقاً یک `statement` + یک `list`.
۵. **شش‌شکلِ بلاغی (بدونِ تغییرِ کد)** — شش بریفِ واقعی با ساختارهای بلاغیِ
   ماهیتاً متفاوت (فرآیندِ پلکانی، روایتِ تکی، توضیحِ تک‌مفهومی، پرسش‌وپاسخ،
   مقایسه/تصمیم، افسانه/بازتعریف) آزموده شد. دو کاروسل طبیعتاً یک چیدمان را
   صفر بار گرفتند (یکی صفر `statement`، یکی صفر `list`)، بدونِ الگوی
   موقعیتیِ ثابت — شواهد به‌سودِ **انتخابِ محتوامحور**، نه سوگیریِ پرامپت.
۶. **هفته‌ی نهایی پاس شد و تنوعِ چیدمان تأیید شد** — جزئیات زیر.

### نتیجه‌ی اعتبارسنجیِ نهاییِ هفتگی

- هفته‌ی جدید و غیرمخربِ `2026-09-05` (بدونِ overwrite هفته‌ای موجود):
  **۷ از ۷ موفق، ~۵۵ ثانیه**، منتقدِ هفته ۸۶/۱۰۰
- **۳۸ اسلاید** — `standard` ۲۶ (۶۸٫۴٪) · `statement` ۵ (۱۳٫۲٪) · `list` ۷ (۱۸٫۴٪)
- موقعیتِ `statement` در هفت کاروسل: `[۴]،[۱]،[]،[]،[۱]،[۲]،[۴]` — پراکنده،
  بدونِ اندیسِ ثابت؛ دو کاروسل طبیعتاً صفر `statement` گرفتند
- موقعیتِ `list`: در اندیس‌های ۲، ۳ و ۴ — پراکنده، بدونِ اندیسِ ثابت
- ایمنی: صفر preprocess fallback (`[slide-layout]`)، صفر شکستِ اسکیمای
  چیدمانِ نامعتبر، صفر تبدیلِ `guardPositionLayout`، یک بریدنِ آرایشیِ
  `clampSlides` (بدونِ اثرِ کیفی — پایین)
- چکِ advisoryِ «تنوعِ چیدمانِ اسلایدهای میانی» در هر هفت پست پاس شد و هیچ
  دورِ بازنویسی به‌خاطرِ تنوعِ چیدمان ساخته نشد
- هزینه‌ی هفت کاور: $۰٫۲۳۸۶ — `npm run build` پاس
- آرتیفکتِ بصری (هر ۳۸ اسلایدِ رندرشده، به‌تفکیکِ پست):
  https://claude.ai/code/artifact/73e6841d-ab9c-43f1-ba2c-2cebf9b0c1fc
- وردیکت: **✅ READY TO CLOSE**

### بدهیِ فنیِ غیرمسدودکننده

`clampText` روی تیترِ خارج‌از‌سقفِ اسلایدِ `statement` در روزِ چهارشنبه یک
بریدنِ آرایشیِ وسطِ جمله ساخت (۴۴→۳۷ کاراکتر، پایانِ سه‌نقطه‌دار). رفتارِ
عمومیِ `clampText` است، نه چیزی مختصِ تنوعِ چیدمان، و جلوی فاز ۴ را نمی‌گیرد.

### هیچ مانعِ باقی‌مانده‌ای برای تنوعِ چیدمان وجود ندارد

معماری (`discriminatedUnion`، `slideText`، `blocksFor`، `normalizeSlide`،
`guardPositionLayout(clampSlides(...))`) قفل است و نباید بدونِ دلیلِ تازه
دوباره باز شود.

### کارِ بعدی: فاز ۴ — استوری

تا کاربر صریحاً نگفته شروعش کن، شروع نشود. طرحِ سطح‌بالا در `MASTER_PLAN.md`
بخش ۴؛ پیش از پیاده‌سازی یک طرحِ تکمیلی لازم است.
