import "server-only";
import { runAgentJSON } from "@/lib/ai";
import { COMPANY_NAME, COMPANY_PROFILE, BRAND_VOICE } from "@/lib/company";
import { lessonsBlockFor } from "./lessons";
import {
  SLIDE_LIMITS,
  InstagramCarouselSchema,
  type InstagramCarousel,
  type SocialBrief,
  type SocialReview,
} from "./types";
import type { SocialCheck } from "./social-checks";

/**
 * ایجنت ۲ (پایپ‌لاین بازآفرینی) — کپی‌رایتر اینستاگرام
 *
 * وظیفه: بریف اجتماعی را به یک کاروسل ۵ تا ۸ اسلایدی + کپشن تبدیل کند.
 *
 * نکته‌ی آموزشی: برخلاف نویسنده‌ی بلاگ که متن آزاد مارک‌داون می‌دهد، این
 * ایجنت خروجی **ساختاریافته** می‌دهد (آرایه‌ای از اسلایدها). چون هر اسلاید
 * بعداً جداگانه در یک قاب ۴:۵ رندر می‌شود، شکل داده باید تضمین‌شده باشد —
 * نه یک رشته که بعداً بخواهیم پارسش کنیم.
 */


/**
 * قواعد ساختار جمله — «زنده در برابر اداری».
 *
 * ⚠️ هر سه مثالِ ❌ **متن واقعیِ تولیدشده‌ی همین سیستم‌اند**، نه نمونه‌ی
 * ساختگی. از ۱۳۰ اسلاید ذخیره‌شده در `social_posts` درآمده‌اند. مثال
 * ساختگی به مدل یاد می‌دهد از چیزی پرهیز کند که خودش هرگز نمی‌نویسد.
 *
 * سه الگو، شمرده‌شده روی همان ۱۳۰ اسلاید:
 * ۱. اسم‌سازی به‌جای فعل — «از دست رفتن دستاورد» به‌جای «از دست بدهید»
 * ۲. مجهول و بی‌فاعل — «باید مشخص شود» به‌تنهایی ۴ بار تکرار شده
 * ۳. جمله‌ی بلند با بند وابسته — ۲۲ اسلاید بالای ۲۰ کلمه
 *
 * ⚠️ اینجا **عمداً چک قطعی ساخته نشد.** تطبیق الگو روی فارسی دقیقاً
 * همان تله‌ی قاعده‌ی ۲ است: «اسم مصدر» گاهی طبیعی‌ترین شکل جمله است
 * («ارزیابی مسیر» نام یک خدمت است، نه ایراد نگارشی)، و «می‌شود» در
 * فارسی همیشه مجهول نیست. چکِ کاذب، بازنویسی بی‌دلیل می‌سازد.
 *
 * BRAND_VOICE از قبل می‌گفت «جمله‌ها کوتاه، بدون زبان اداری» — یک جمله
 * در یک بلوک بلند. آنچه کم بود، الگوی مشخص و مثال قبل/بعد بود.
 */

/**
 * ⚠️ درس مثال‌ها: مثالِ ✅ به همان اندازه‌ی مثالِ ❌ تقلید می‌شود.
 *
 * نسخه‌ی اول قاعده‌ی `imageSubject` دو مثال ✅ داشت که هر دو صحنه‌ی
 * میز کار بودند («دفترچه‌ی باز روی میز»، «فنجان قهوه کنار لپ‌تاپ»).
 * در دو اجرای واقعیِ پشت‌سرهم، مدل برای دو موضوع کاملاً متفاوت هر دو بار
 * «لپ‌تاپ روی میز» داد. مثال‌ها سقفِ تنوع را تعیین کردند، نه کفش را.
 *
 * پس مثال‌های ✅ حالا عمداً از چهار دنیای متفاوت‌اند و هرکدام به یک
 * گروه مخاطب چسبیده‌اند. اگر روزی مثال جدیدی اضافه کردی، همین را نگه دار:
 * مثال هم‌جنس، خروجی هم‌جنس می‌سازد.
 *
 * ⚠️ و این تضمینِ تنوع **در سطح هفته** نیست. هفت اجرای موازی همدیگر را
 * نمی‌بینند — همان کوریِ ضدتکرار که در weekly-planner.ts توضیح داده شده.
 * راه‌حل ساختاری، یک ستون «خانواده‌ی صحنه» در WEEKLY_GRID است تا مثل
 * route و language قطعی و از پیش تقسیم‌شده باشد. هنوز ساخته نشده.
 */

/**
 * قواعد مخصوص زبان خروجی.
 *
 * ⚠️ انگلیسی «ترجمه‌ی» فارسی نیست. قاعده‌ی صریح برندگاید: ساختار ترجمه
 * می‌شود، جمله‌ها از نو نوشته می‌شوند. اگر فقط بگوییم «به انگلیسی بنویس»،
 * خروجی انگلیسیِ ترجمه‌شده از فارسی می‌شود که برای مخاطب بریتانیایی
 * مصنوعی می‌خواند — و آن مخاطب نهاد و شریک بالقوه است، نه متقاضی.
 */
const FA_BLOCK = `
زبان خروجی: **فارسی**.
- اعداد داخل متن را فارسی بنویس.
- هشتگ‌ها ترکیبی از فارسی و انگلیسی.

**ساختار جمله — این بند را جدی بگیر.** متنِ درست ولی بی‌جان بدترین حالت است: هیچ ایرادی ندارد و هیچ‌کس هم نمی‌خواندش. سه مثال زیر خروجی واقعی همین سیستم‌اند.

۱. **فعل بنویس، نه اسمِ فعل.**
❌ «دور شدن از اجرای مستقیم فنی به معنای از دست رفتن دستاورد نیست. تاثیر شما به سطح تصمیم و هدایت منتقل شده است.»
✅ «از کد فاصله گرفته‌اید، ولی دستاوردتان را از دست نداده‌اید. حالا در سطح تصمیم اثر می‌گذارید.»
چه عوض شد: «دور شدن» و «از دست رفتن» شدند فعل، و «منتقل شده است» فاعل گرفت.

۲. **فاعل را در جمله نگه دار.** «سنجیده می‌شود»، «باید مشخص شود»، «مستند نشده است» — چه کسی می‌سنجد؟ چه کسی باید مشخص کند؟
❌ «عنوان شغلی ارشد نشان‌دهنده‌ی تناسب پرونده نیست؛ وزن شواهد با میزان اثرگذاری روی محصول سنجیده می‌شود.»
✅ «عنوان شغلی ارشد پرونده نمی‌سازد. ارزیاب می‌پرسد روی محصول چه اثری گذاشته‌اید.»

۳. **جمله‌ی بلند را با نقطه بشکن.** بالای ۲۰ کلمه با «که» و «حتی اگر» یعنی دو جمله را به‌زور یکی کرده‌ای.
❌ «تک‌لیدی که پایداری سیستم را به ۹۹٫۹٪ رسانده، تصمیم معمارانه گرفته است؛ حتی اگر تمام کدها را اعضای تیمش کامیت کرده باشند.»
✅ «سیستم را به پایداری ۹۹٫۹٪ رساندید. کد را تیم زد، معماری را شما انتخاب کردید.»

⚠️ این یعنی «ساده‌نویسی»، نه «سطحی‌نویسی». دقتِ فنی و عدد سر جایشان می‌مانند؛ فقط جمله باید انگار آدمی آن را گفته باشد.`;

const EN_BLOCK = `
زبان خروجی: **انگلیسی**. کل خروجی — عنوان، کپشن، تیتر و متن اسلایدها، هشتگ‌ها و CTA — باید انگلیسی باشد. هیچ جمله‌ی فارسی در خروجی نباشد.

این ترجمه نیست. از نو به انگلیسی بنویس:
- جمله‌های انگلیسی کوتاه‌ترند. جمله‌ی فارسیِ ترجمه‌شده در انگلیسی بلند و پیچیده می‌شود — نکن.
- مخاطب انگلیسی‌زبان اینجا با مخاطب فارسی فرق دارد: نهادها، دانشگاه‌ها، همکاران بین‌المللی و متخصصان غیرایرانی. لحن حرفه‌ای و مستقیم، بدون صمیمیت محاوره‌ای.
- اصطلاحات رسمی را به شکل انگلیسیِ خودشان بنویس: Global Talent visa، Innovator Founder visa، endorsement، Indefinite Leave to Remain (ILR).
- اعداد را لاتین بنویس.
- هشتگ‌ها همه انگلیسی.
- هرگز «lawyer»، «solicitor» یا «legal advice» ننویس — این‌ها در بریتانیا عناوین حفاظت‌شده‌اند. اگر لازم شد، «immigration advice» درست است.

**ساختار جمله:** متنِ درست ولی بی‌جان بدترین حالت است. سه قاعده:
- فعل بنویس، نه اسمِ فعل: «you lose your achievement» ✅ در برابر «the loss of your achievement» ❌.
- فاعل را نگه دار: «assessors look for…» ✅ در برابر «is assessed by…» / «must be determined» ❌.
- جمله‌ی بلند با بند وابسته را با نقطه بشکن.`;

/**
 * بند خانواده‌ی صحنه — فقط در مسیر هفتگی پر می‌شود.
 *
 * ⚠️ خالی‌بودنش حالت عادی است، نه خطا: اجرای تکی از استودیو برنامه‌ریز
 * ندارد، پس خانواده‌ای هم ندارد و قاعده‌های عمومی `imageSubject` کف کار
 * می‌مانند.
 *
 * «داخل این خانواده» می‌گوییم و نه «دقیقاً همین صحنه» — خانواده تنوعِ
 * سطح هفته را تضمین می‌کند، ولی صحنه‌ی مشخص را کپی‌رایتر می‌سازد که
 * برخلاف برنامه‌ریز محتوای واقعی کاروسل را دیده است.
 */
function sceneBlock(sceneFamily?: string | null): string {
  if (!sceneFamily?.trim()) return "";
  return `

**خانواده‌ی صحنه‌ی این کاور: «${sceneFamily.trim()}»**
«imageSubject» باید داخل همین خانواده بماند — همین فضا، همین جنس نور، همین دنیای بصری. جزئیاتش را خودت متناسب با موضوع همین کاروسل بساز؛ کپیِ لفظ‌به‌لفظِ جمله‌ی بالا را برنگردان.
این خانواده برای تمایز کاورهای یک هفته از هم انتخاب شده، پس از آن خارج نشو.`;
}

/** قواعد مشترک بین پیش‌نویس اول و بازنویسی */
function systemPrompt(lessons: string, language: "fa" | "en", sceneFamily?: string | null): string {
  return `تو «کپی‌رایتر اینستاگرام» ${COMPANY_NAME} هستی. کاروسل‌های آموزشی می‌نویسی که مخاطبِ توصیف‌شده در پروفایل شرکت اسکرول را برایشان متوقف کند.

${COMPANY_PROFILE}

${BRAND_VOICE}

قواعد اینستاگرام (رعایتشان اجباری است):
- **جمله‌ی اول کپشن** قلاب است و باید کوتاه‌تر از ۱۲۵ کاراکتر باشد؛ اینستاگرام حدوداً همان‌جا «... بیشتر» می‌زند. این جمله باید به‌تنهایی معنی بدهد و کنجکاوی بسازد، و با نقطه یا علامت سؤال تمام شود. با سلام، با نام برند، یا با «در این پست...» شروع نکن.
- کاروسل ۵ تا ۸ اسلاید دارد و **نقش هر اسلاید از جایگاهش تعیین می‌شود، نه انتخاب تو**:
  · اسلاید **اول** = قلاب. تصویریِ همان ایده، نه کپیِ لفظ‌به‌لفظِ کپشن.
  · اسلایدهای **میانی** = هر کدام دقیقاً یک ایده.
  · اسلاید **آخر** = دعوت به اقدام. این نقش قطعی است؛ رندرکننده این اسلاید را با رنگ «اقدام» می‌کشد چه محتوایش دعوت باشد چه نباشد.
- **kicker باید نقشِ همان اسلاید را منعکس کند.** kicker یک برچسب است، نه شماره.
  · روی اسلاید آخر kicker باید دعوت را اعلام کند: «قدم بعدی»، «از اینجا شروع کنید»، «حالا چه؟».
  · ⚠️ روی اسلاید آخر **هرگز** برچسب دنباله‌ای نگذار — «قدم اول»، «قدم دوم»، «۱ از ۵». در یک اجرای واقعی اسلاید ۶ از ۶ کیکر «قدم اول» گرفت در حالی که با رنگ اقدام رندر شده بود؛ خواننده هم‌زمان «شروع» و «پایان» را می‌دید.
  · اگر برچسب دنباله‌ای به کار می‌بری، ترتیبش باید با ترتیب واقعی اسلایدها بخواند و روی اسلاید آخر تمام شده باشد.
- heading هر اسلاید باید در اندازه‌ی بندانگشتی خوانا باشد: کوتاه، بدون جمله‌ی وابسته. text حداکثر دو جمله‌ی کوتاه.
- **سقف‌های سخت هر اسلاید — این‌ها شمرده می‌شوند، حدس نیستند:** kicker حداکثر ${SLIDE_LIMITS.kicker} کاراکتر، heading حداکثر ${SLIDE_LIMITS.heading}، text حداکثر ${SLIDE_LIMITS.text}. رد شدن از این‌ها محتوا را برای بازنویسی برمی‌گرداند. **کوتاه بنویس، نه اینکه بلند بنویسی.** این متن‌ها روی تصویر می‌نشینند و جای اضافه‌ای وجود ندارد.
- **هیچ لینکی در کپشن نگذار.** اینستاگرام لینک کپشن را کلیک‌پذیر نمی‌کند؛ به‌جایش بنویس «لینک در بایو».
- ۳ تا ۵ هشتگ، هرکدام با # و بدون فاصله، بدون تکرار. هشتگ اسپم ممنوع (#فالو، #لایک، #فالوبک).
- ایموجی کم و کاربردی. لحن برند اجازه‌ی لحن تبلیغاتی داغ نمی‌دهد.
- **فقط برای اسلاید اول**، فیلد «imageSubject» را پر کن: یک **صحنه‌ی فیزیکیِ قابل عکاسی** که پس‌زمینه‌ی کاور شود. یک جمله، فارسی.
  · باید چیزی باشد که بشود از آن **عکس گرفت**: شیء، مکان، نور، بافت. نه یک مفهوم.
  · ⚠️ **آزمون تمایز — مهم‌ترین بند:** اگر همین صحنه برای هر موضوع دیگری از این پیج هم به همان اندازه جواب می‌دهد، **رد است**. صحنه باید از دنیای واقعیِ همین مخاطب و همین موضوع بیرون آمده باشد؛ از شیئی که او هر روز دستش می‌گیرد یا جایی که واقعاً کار می‌کند.
  · ❌ «یک لپ‌تاپ روی میز کار کنار فنجان قهوه» — **در دو اجرای پشت‌سرهم دقیقاً همین آمد.** برای هر موضوعی جواب می‌دهد، پس هیچ موضوعی را نمی‌گوید. میز کار، لپ‌تاپ، دفترچه و فنجان قهوه به‌تنهایی تقریباً همیشه یعنی صحنه‌ی عمومی.
  · ❌ «شواهد قابل بررسی» — مفهوم است، صحنه نیست. مدل تصویرساز نمی‌داند با آن چه کند.
  · ❌ «موفقیت در مسیر مهاجرت» — انتزاعی و کلیشه‌ای.
  · ✅ «یک بوم نیمه‌کار روی سه‌پایه در کارگاه خالی، نور از پنجره‌ی سقفی» — برای مخاطب هنر و فرهنگ.
  · ✅ «دفترچه‌ی آزمایشگاه با نمودار دست‌نویس، کنار یک میکروسکوپ قدیمی» — برای مخاطب پژوهشی.
  · ✅ «کلاه ایمنی روی نرده‌ی فلزی، پشت‌بام یک ساختمان نیمه‌کاره در نور صبح» — برای مخاطب مهندسی.
  · ✅ «یک وایت‌برد پاک‌شده با ردهای محو ماژیک، اتاق جلسه‌ی خالی» — برای مخاطب کارآفرین.
  · صحنه باید **خلوت** باشد: یک عنصر اصلی، بدون شلوغی. متن روی همین تصویر می‌نشیند.
  · هیچ آدمی با چهره‌ی مشخص، هیچ پرچم و پاسپورت و چمدان و هواپیما، و هیچ متنی داخل صحنه نباشد.
  · **هیچ نمایشگر روشنی هم نباشد** — مانیتور، صفحه‌ی موبایل، نمودار و رابط کاربری. سبک تصویر این‌ها را ممنوع کرده و صحنه‌ای که رویشان بنا شده باشد خالی درمی‌آید.
  · اگر برای این اسلاید صحنه‌ی خوبی به ذهنت نمی‌رسد، **این فیلد را خالی بگذار**. کاور بدون تصویر بهتر از تصویر بی‌ربط است.
${language === "fa" ? FA_BLOCK : EN_BLOCK}${sceneBlock(sceneFamily)}${lessons}`;
}

function briefBlock(brief: SocialBrief): string {
  return `بریف اجتماعی:
پیام مرکزی: ${brief.coreMessage}
مخاطب: ${brief.audience}
زاویه‌ی قلاب: ${brief.hookAngle}
نکته‌های کلیدی:
${brief.keyPoints.map((p) => `- ${p}`).join("\n")}
شاهد/مثال: ${brief.proofPoint}
دعوت به اقدام: ${brief.cta}`;
}

const SHAPE_HINT = `{
  "title": "عنوان داخلی برای فهرست استودیو",
  "caption": "کپشن — ۱۲۵ کاراکتر اولش قلاب است",
  "slides": [
    { "kicker": "قلاب", "heading": "تیتر کوتاه اسلاید", "text": "یکی دو جمله", "imageSubject": "صحنه‌ی فیزیکی — فقط اسلاید اول" }
  ],
  "hashtags": ["#گلوبال_تلنت", "#GlobalTalent", "#مهاجرت_حرفه‌ای"],
  "cta": "دعوت به اقدام اسلاید آخر"
}`;

const SHAPE_HINT_EN = `{
  "title": "short internal title",
  "caption": "first sentence is the hook, under 125 characters",
  "slides": [
    { "kicker": "hook", "heading": "short slide heading", "text": "one or two short sentences", "imageSubject": "a physical, photographable scene — first slide only" }
  ],
  "hashtags": ["#GlobalTalent", "#UKVisa", "#TechTalent"],
  "cta": "call to action on the last slide"
}`;

export async function runInstagramWriter(input: {
  brief: SocialBrief;
  /** خانواده‌ی صحنه‌ی کاور — فقط مسیر هفتگی پرش می‌کند */
  sceneFamily?: string | null;
}): Promise<InstagramCarousel> {
  const lessons = await lessonsBlockFor("instagram-writer");

  return runAgentJSON({
    agent: "instagram-writer",
    system: systemPrompt(lessons, input.brief.language, input.sceneFamily),
    prompt: `${briefBlock(input.brief)}

${input.brief.language === "en" ? "Write a complete Instagram carousel in English." : "یک کاروسل اینستاگرام کامل بنویس."}`,
    temperature: 0.8,
    schema: InstagramCarouselSchema,
    shapeHint: input.brief.language === "en" ? SHAPE_HINT_EN : SHAPE_HINT,
  });
}

/**
 * بازنویسی بر اساس ایرادهای ویراستار + چک‌های قطعیِ ردشده.
 *
 * نکته: پیش‌نویس قبلی را کامل به مدل می‌دهیم تا «اصلاح» کند، نه اینکه از
 * صفر بنویسد؛ وگرنه چیزهایی که درست بودند هم عوض می‌شوند.
 */
export async function runInstagramRevision(input: {
  brief: SocialBrief;
  draft: InstagramCarousel;
  review: SocialReview;
  failedChecks: SocialCheck[];
  /**
   * ⚠️ بازنویسی هم باید خانواده را بگیرد. بدون این، دور بازنویسی قید
   * تنوع را بی‌صدا می‌انداخت و کاور دوباره به صحنه‌ی عمومی برمی‌گشت.
   */
  sceneFamily?: string | null;
}): Promise<InstagramCarousel> {
  const lessons = await lessonsBlockFor("instagram-writer");

  const prompt = `${briefBlock(input.brief)}

— پیش‌نویس فعلی —
${JSON.stringify(input.draft, null, 2)}

— ایرادهای ویراستار (امتیاز ${input.review.score}/100) —
${input.review.issues.map((i) => `- ${i}`).join("\n") || "- (بدون ایراد)"}

— چک‌های قطعیِ ردشده —
${input.failedChecks.map((c) => `- ${c.name}: ${c.note}`).join("\n") || "- (همه پاس شدند)"}

کاروسل را اصلاح کن. فقط چیزهایی را عوض کن که ایراد دارند؛ بقیه را دست نزن.`;

  return runAgentJSON({
    agent: "instagram-writer",
    system: systemPrompt(lessons, input.brief.language, input.sceneFamily),
    prompt,
    temperature: 0.6,
    schema: InstagramCarouselSchema,
    shapeHint: input.brief.language === "en" ? SHAPE_HINT_EN : SHAPE_HINT,
  });
}
